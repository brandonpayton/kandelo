# N1-I5: Native fork REFERENCE reconstruction via the shared co-resident module

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Checkbox (`- [ ]`) steps. **READ the grounding doc first: `docs/plans/2026-09-05-n1-i5-references-grounding.md`** — it has every export signature, file:line anchor, and the Node/browser wiring to mirror. This plan is the task breakdown; the grounding doc is the reference.

**Goal (Milestone M2):** Make native fork REFERENCE reconstruction LIVE through the SAME shared `fork_module32.wasm` that I4 wired for frames. A fork that captures references (funcref + externref + an anyref-transit value) reconstructs them correctly in the child, with externref identity preserved. The module owns the algorithm (Path B); native only populates the module's imported tables, wires the guest's reference imports to the module, provides `resolve_externref` + the `ForkHostCapabilities` tag methods, and drives the reference coordinator in order. NO host-specific reconstruction logic. Frames (I4) already work; this is additive.

**Scope boundary (do NOT cross into M3/F6):** I5 = the wires are live and carry current for the EASY kinds (funcref via catalog `table.get`; externref via `resolve_externref`; the anyref transit plumbing working end-to-end via a static-root or a simple GC-drive step), with identity proven and `ForkHostCapabilities` no longer `ENOSYS`. I5 does NOT remove the `EOPNOTSUPP` gates for GC struct/array/i31/GC-derived-externref/full-static-root-graphs — that is M3/F6, one kind at a time, on the substrate I5 delivers. (Grounding §7.)

**Architecture:** co-resident module (I4) unchanged; I5 swaps the four reference imports from empty/trapping (I4 Task 1's inert stubs, `guest.rs:2661-2709`) to live, wires the guest's own reference imports for the first time, and drives the `fm_*` reference coordinator. All reconstruction stays in the module + `fork-codec`-in-wasm. (Grounding §6.)

**Tech Stack:** Rust + `wasmtime = "48"` (GC + exceptions enabled), the co-resident `fork_module32.wasm`, Kandelo SDK + `scripts/run-wasm-fork-instrument.sh` for the fixture.

**Spec:** `docs/plans/2026-09-05-rust-first-campaign-to-completion.md` §B2/M2 + the grounding doc.

## Global Constraints

- Worktree `/Users/brandon/kandelo-abi44-reconcile` (branch `brandonpayton/rust-first-abi44-reconcile`; PR #1350 head `brandonpayton/epoll-kernel-route`). Dev-shell + cache: `export KANDELO_SOURCE_CACHE_ROOT="$HOME/.cache/kandelo/reconcile-abi44"; scripts/dev-shell.sh bash -lc '<cmd>'`.
- Test: `cargo test -p host-native --target $(rustc -vV|sed -n 's/^host: //p')` (host `aarch64-apple-darwin`).
- **Host-side-only in `crates/host-native`** (+ a native `impl ForkHostCapabilities`, + the fixture). NO kernel/runtime-core/abi/shared/glue/fork-instrument/fork-module change. The module + fork-codec are the shared algorithm — do NOT modify them, do NOT reimplement reconstruction in Rust.
- **Frames (I4) + all pre-existing tests stay green** (33/33 at BASE). Reuse I4's `instantiate_fork_module` (Module load, PIC placement, memory sharing, frame `fm_*` bindings) as-is — I5 is additive to it.
- **Reference kinds beyond I5's three (GC struct/array/i31, GC-derived externref, full static-root graphs) stay `EOPNOTSUPP`/gated** — that's M3. Do not remove those gates.
- Fixture SDK-built + fork-instrumented (commit `.c` + instrumented `.wasm`). Instrumented guests need the `wpk_fork_module_bootstrap` call I4 added — reuse that path.
- If a wasmtime-48 GC/table/ref API genuinely cannot express a needed step (e.g. reading/copying an `anyref` table entry, minting a `Tag`, rooting an `ExternRef`), report BLOCKED with the exact API gap — do NOT hack or reimplement in the module.

---

### Task 1: Wire the module's reference imports live + guest reference-import flip
**Files:** `crates/host-native/src/guest.rs` (extend `instantiate_fork_module` + the `ForkModule` struct + the guest-instantiation path). **Grounding: §2, §4 (funcref/drive/static-root mirror + gc_transit bind + import flip), §6 deliverables 1-4 + 6-binding.**

- [ ] **Step 1: Bind the new `fm_*` reference exports into `ForkModule`** (grounding §6 deliverable 6-binding + §1 signatures): `fm_begin_reference_replay`, `fm_set_activation_catalog_base`, `fm_set_activation_static_root_base`, `fm_set_activation_gc_codec`, `fm_set_host_exception_owner`, `fm_build_gc_plan`, `fm_gc_plan_count`, `fm_drive_execute` (injected export), `fm_drive_table_base`, the seven `fm_ref_*`, `fm_funcref_ordinal`/`fm_static_root_slot`/`fm_externref_handle`, and read the `__wpk_fork_ref_gc_transit` **table export**. Bind read-taps for the reference proof-of-use counters (`fm_references_reconstructed`, `fm_externrefs_resolved`, `fm_exnrefs_reconstructed`, `fm_gc_nodes_reconstructed`, `fm_static_roots_published`, `fm_drive_steps_executed`).
- [ ] **Step 2: Populate the three module-imported tables from the guest's exports** (grounding §4 + §6 deliverable 1-2), once the guest instance(s) exist: `__wpk_fork_function_catalog` (`Table::grow`+`set` real `Func`s copied from each guest's exported catalog; `fm_set_activation_catalog_base` for >1 activation); `__wpk_fork_drive_table` (the guest's `_gc_allocate`/`_gc_fill`/`_exception_materialize` `Func`s at `fm_drive_table_base(act)+{0,1,2}`); `__wpk_fork_static_root_catalog` (child's live static-root values; `fm_set_activation_static_root_base` for >1). (For the I5 fixture, single-activation — base seeding is the simple default; multi-activation/dlopen is exercised later.)
- [ ] **Step 3: Bind the module's `__wpk_fork_ref_gc_transit` export into the GUEST's import** `env.__wpk_fork_ref_gc_transit` (grounding §2 row 4, §6 deliverable 3) — net-new; no native wiring exists. Guest is instantiated after the module (I4 ordering), so read the module's exported table and supply it in the guest's import object.
- [ ] **Step 4: Flip the guest's reference imports to the module's exports** (grounding §4 import-flip, §6 deliverable 4): `__wpk_fork_ref_decode_funcref`/`_decode_externref` → the module's shim exports; the seven `__wpk_fork_ref_{vector_get,gc_route,gc_payload_len,gc_load,exn_route,exn_load,exn_cache_index}` → the `fm_ref_*` exports. Synchronous `func_wrap`/linker rebind (simpler than Node's async merge). Only when the guest is fork-instrumented (`fork_format`); un-instrumented guests unaffected.
- [ ] **Step 5:** Build (compiles) + full suite still 33/33 (nothing drives references yet, so the frame tests + the 31 pre-existing stay green; this task is wiring). Commit: `git commit -m "Host-native: wire the co-resident module's reference imports + guest reference-import flip (N1-I5)"`

### Task 2: `resolve_externref` + `ForkHostCapabilities` tag methods
**Files:** `crates/host-native/src/guest.rs` (the `env.resolve_externref` Func + the handle→`Rooted<ExternRef>` map/RootScope); a native `impl ForkHostCapabilities` replacing `fork_codec::native_sketch::NativeForkHost`. **Grounding: §3, §5, §6 deliverables 5-6.**

- [ ] **Step 1: Implement `env.resolve_externref(handle: i32) -> externref`** as a real `Func` (replace the trapping stub, `guest.rs:2700-2709`) over a `HashMap<u32, Rooted<ExternRef>>` scoped by a `RootScope` per fork generation — **idempotent per handle** (same `Rooted<ExternRef>` for repeat asks; never `ExternRef::new` twice for one handle). Grounding §5 is the spec.
- [ ] **Step 2: Implement the 3 `ForkHostCapabilities` methods** (`mint_exception_tag`/`provide_unwind_transport_tag`/`recognize_unwind_transport`) via wasmtime 48 `Tag::new`/`Tag::eq` (per `native_sketch.rs`'s mapping table), replacing the `ENOSYS` bodies. NOTE (grounding §3): none of these is on the funcref/externref/GC reference-replay hot path — they're unwind-transport/native-exception plumbing; de-ENOSYS them per the plan, but I5's reference acceptance does not depend on them firing. If `provide_unwind_transport_tag` turns out to be needed by native's existing unwind path, wire it; else it stays a real-but-unexercised method.
- [ ] **Step 3:** Build + 33/33 still green. Commit: `git commit -m "Host-native: resolve_externref (idempotent Rooted<ExternRef> map) + ForkHostCapabilities tags (N1-I5)"`

### Task 3: Drive the reference coordinator + the e2e fork-with-references fixture
**Files:** `crates/host-native/src/guest.rs` (the reference-replay drive sequence in the fork path); `crates/host-native/fixtures/native_fork_refs.c` (+ instrumented `.wasm`); test in `crates/host-native/src/lib.rs`. **Grounding: §1 DRIVE model, §4, §6 deliverable 7-8, §7 (what counts as "reconstructed").**

- [ ] **Step 1: Write the failing test.** `native_fork_refs.c`: hold a funcref (e.g. a function pointer in a table/local), an externref (from a host-provided externref-producing path — determine the simplest native way to get one into the guest), and a value that exercises the anyref transit (a static-root or a simple GC value), live ACROSS `fork()`; child + parent both verify the reference survived (e.g. call through the funcref, compare the externref identity, read the transit value) and exit with a marker code proving correctness. Build + fork-instrument it (`scripts/run-wasm-fork-instrument.sh`; confirm imports resolve via `Module::imports()`). `#[test] smoke_fork_reconstructs_references`: assert the marker exit code AND `fm_references_reconstructed`/`fm_externrefs_resolved` (+ `fm_drive_steps_executed`/`fm_static_roots_published` if the fixture exercises the transit) all `> 0`. Fails first (references not driven → wrong marker / counters 0 / trap).
- [ ] **Step 2: RED** — run it.
- [ ] **Step 3: Implement the drive sequence** (grounding §1 "How the guest feeds references" + §4 + `fork-process-continuation.ts:1081-1100`): in the fork path, after `fm_begin_child_replay`'s host prep and BEFORE the guest touches a reference: `fm_set_activation_gc_codec` + `fm_set_host_exception_owner` (seed) → `fm_begin_reference_replay(module_state_root, pid)` → `fm_build_gc_plan(pid)` → `fm_drive_execute(plan_ptr, count)` (the injected loop drives ALLOC/FILL/EXN via the drive table + STATIC_ROOT/EXTERNREF_TRANSIT via `fm_static_root_slot`/`fm_externref_handle`+`resolve_externref`) → then the rewind (already there from I4) whose guest codec pulls via the flipped decode/feed imports. `fm_last_errno` after each stage → truthful failure. Reuse the frame path unchanged; this adds the reference sub-sequence.
- [ ] **Step 4: Add the externref-identity check** (grounding §5, plan Step 3 acceptance): the fixture forks a program holding one externref handle referenced from two recipe sites; assert both reconstructed sites yield an `ExternRef` whose backing Rust value (`.data(&store)`) is the SAME — proving idempotent-per-handle materialization on wasmtime (empirically, since the floor was found empirically).
- [ ] **Step 5: GREEN** (references reconstruct, identity holds, counters > 0, frames-only tests + 31 pre-existing still green) + full suite. Commit: `git commit -m "Host-native: drive the fork reference coordinator; references reconstruct across a native fork (N1-I5)"`

---

## Notes for the executor
- The grounding doc (`docs/plans/2026-09-05-n1-i5-references-grounding.md`) is authoritative for every signature + the exact Node/browser sequence to mirror; read it before each task.
- The reconstruction ALGORITHM is the module's — you wire tables, flip imports, provide `resolve_externref`/tags, and drive `fm_*` in order. If you find yourself decoding recipes or materializing GC objects in Rust, stop: that's the module's job.
- `fm_*_reconstructed > 0` is the anti-silent-fallback proof: a flag-off/JS fallback leaves them 0. The identity check + marker exit code are the correctness proof.
- Keep the I5/M3 boundary: prove funcref + externref + the anyref transit mechanism; do NOT remove the struct/array/i31/GC-derived/full-static-root `EOPNOTSUPP` gates (M3).
- Report BLOCKED (not a hack) on a genuine wasmtime-48 GC/ref/table/Tag API wall; that's a legitimate strong-doubt escalation.
- Reuse I4's `instantiate_fork_module` + `wpk_fork_module_bootstrap` (instrumented-guest table init) + `reclaim_parked_thread` (teardown). Fixtures SDK-built + instrumented; commit `.c` + `.wasm`.
