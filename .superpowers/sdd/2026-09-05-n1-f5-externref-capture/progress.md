# SDD ledger — plan: docs/superpowers/plans/2026-09-05-n1-f5-externref-capture.md

BASE (pre-Task-1): e45d988fa (F5 grounding + plan docs commit, on I5b 410ee76e5)
Worktree: /Users/brandon/kandelo-abi44-reconcile (branch brandonpayton/rust-first-abi44-reconcile; PR #1350 head = brandonpayton/epoll-kernel-route)
F5 = lift the externref EOPNOTSUPP gate on ALL hosts via FLOOR-1 production-site provenance recording (NOT the deleted unsound reverse-lookup). Organizing lens = capture↔replay symmetry (campaign plan Decision 2a; [[capture-replay-symmetry-lens]]): replay is the spec, capture is its inverse; the wire writer intern_externref already exists; the only real work is value→handle provenance recorded at the mint site.
SCOPING RULING (F5/F6 boundary): F5 = host-imported externref. Guest-internalized externref (extern.internalize on a guest anyref, no host call) = F6 (it's a GC-provenance problem). Until F6, internalize-externref stays cleanly EOPNOTSUPP.

## Progress
Task 1: DONE (agent a07364e9291b7c817; commit f8bb088ed). fork-instrument externref production-site provenance-wrapper pass + new ABI import.
  - crates/shared: WPK_FORK_REFERENCE_IMPORT_PROVENANCE_EXTERNREF = "__wpk_fork_ref_provenance_externref", added to WPK_FORK_REQUIRED_IMPORTS (45→46), signature fn(externref)->externref (pass-through). Stayed ABI 44 (additive within open epoch).
  - crates/fork-instrument/src/externref_provenance.rs (new): finds externref-result host imports, wraps every DIRECT call site (call real import → call provenance import → yield value). Wired into instrument() after fork-path validation, before other import-injecting passes. Always declares the import (uniform required-import surface).
  - abi/snapshot.json + host/src/generated/abi.ts regenerated (scripts/check-abi-version.sh update); diff = exactly the one new import.
  - 6 new tests. CONTROLLER-VERIFIED GREEN (bg bo6wo63rq): 93 fork-instrument tests pass (14+8+3+35+3+21+9), 0 failed; check-abi-version.sh = "snapshot in sync with sources" + "ABI_VERSION and snapshot are consistent" (ABI 44, additive).
  - RESIDUAL GAP (agent-flagged, documented in externref_provenance.rs): call_indirect/call_ref-TO-an-import bypasses the direct-call wrapper → such an externref gets NO provenance. Same shape as the extern.internalize residual. **T2/T3 MUST make CAPTURE cleanly EOPNOTSUPP on any externref lacking recorded provenance — never silently mis-capture.** This is the soundness guard; carry it into every capture-body dispatch.
Task 1 report: .superpowers/sdd/2026-09-05-n1-f5-externref-capture/task-1-report.md

Task 2: PARTIAL/BLOCKED → MAJOR ARCHITECTURAL FINDING (agent a8db483f13467625d; commits a41d6217d WIP + 9f361364c final; 38 pass, 1 ignored, no regression). CONTROLLER-VERIFIED the two load-bearing claims against code:
  - externref capture is NOT a separable path: fork-instrument's emit_externref_bridge (module_gc_codec.rs:1100) + ReferenceCodecOverrides.externref=Some (lib.rs:472) delegate externref through the anyref/GC bridge → the raw __wpk_fork_ref_encode_externref host import is NEVER declared (dead code). The REAL capture gate a plain externref hits is gc_lookup (shared with GC).
  - DECODE-SIDE GAP (replay NOT fully built after all): build_drive_plan (drive_plan.rs:413, doc :405) schedules DRIVE_OP_EXTERNREF_TRANSIT ONLY for GC/exnref-reachable externrefs, NOT directly-held (frame-vector) ones; the "lazy direct decode" path is dead. So a plain externref local across fork has no working decode today.
  IMPLICATIONS: (a) F5/F6 clean split does NOT hold at machinery level — externref rides the shared anyref/GC gc_lookup + drive-plan. (b) F5 is CROSS-CRATE, not host-native-only. (c) Symmetry-lens refinement: replay is complete for transit-reachable externref but has a HOLE for directly-held → F5 completes replay's inverse (build_drive_plan fix) AND adds capture provenance.
  T2 BANKED (sound, kept, forward-compatible-but-currently-dead until gc_lookup wired): ExternrefProvenance (identity-keyed via Rooted::ref_eq, mint-site only) + soundness guard (provenance miss → gated). gc_lookup capture short-circuit was PROTOTYPED + empirically made capture succeed, then REVERTED (decode gap would regress the clean gate into a trap). Recoverable from task-2-report.md concern #2.
  PRE-EXISTING I5b BUG (not F5-introduced): a GATED externref fork hangs today (fm_build_gc_plan failed: errno 22 + 30s timeout) → "parent survives gated fork" is untested. Triage separately.
  Task 2 report: task-2-report.md

RE-SCOPE RULING (controller, 2026-09-05): F5 is a CROSS-CRATE increment. Real work = (1) fork-codec build_drive_plan: schedule transit-publish (or a wired lazy direct-decode) for directly-held/frame-vector-reachable Externref recipes [the replay inverse]; (2) re-apply the prototyped gc_lookup capture short-circuit (peek gc_transit_table slot → ExternRef::convert_any → ExternrefProvenance identity lookup → intern_externref on hit, gate on miss); (3) fork-instrument as needed; (4) host-native test green (native_fork_externref_reconstruct.wat un-ignored); (5) Node/browser parity; (6) fix the pre-existing I5b gate hang (errno 22); (7) docs. Re-ground the decode-side drive-plan fix first (delicate — R1 rooting hazard noted at drive_plan.rs:93). This de-risks F6 (GC reuses the same drive-plan/gc_lookup substrate).
RE-SCOPE APPROVED BY USER (2026-09-05) = Option B (merge F5+F6 into one substrate-first reference-completeness increment; campaign plan c4a9d9eda). User granted AUTONOMY to campaign completion unless serious doubt.

Substrate grounding: DONE (agent af29244e909364280; doc 2026-09-05-n1-refcomplete-decode-substrate-grounding.md, committed 3083725c3). Root causes + minimal fixes:
  - DECODE gap: build_drive_plan Phase 0b (drive_plan.rs:446-463) publishes DRIVE_OP_EXTERNREF_TRANSIT only for reachability-walked (GC/exnref-reachable) externrefs. FIX = publish EVERY Externref node (strict superset; intern_externref dedupes; R1 mechanism unchanged; runs in fork_module32.wasm → all hosts on rebuild).
  - GATE HANG: gated-abort drove fm_build_gc_plan → EINVAL(22) (host-native never seeds GC codecs, guest.rs:5552) → thread dies without resuming → run_pump 30s hard_cap (guest.rs:6737). FIX = skip fm_build_gc_plan/fm_drive_execute on gated-abort → straight to fm_begin_replay/rewind.
  - GC struct/array/i31 already iterate all nodes → no gap; fix is externref-specific + validates shared substrate without entangling F6 capture gate (orthogonal).

Substrate impl: dispatched (sonnet, agent ab7ead30dda0318ce, BASE 3083725c3, monitor bht7odn2c) — 3 coupled fixes (decode Phase 0b widen + fork_module32.wasm rebuild; gate-hang skip; re-apply gc_lookup capture short-circuit from task-2-report.md#2) + un-ignore smoke_fork_externref_reconstructs + add a gated-fork-survives test. Cross-crate (fork-codec + host-native), no ABI-surface change (decode behavior only; run check-abi-version to confirm). commit-early.
NEXT after substrate green: (2) FLOOR-2 GC constructor provenance + un-gate struct/array/i31/static-root [grounded separately; the hard whole-program transform]; (3) Node/browser parity for externref+GC; (4) docs; (5) fork-package rebuild. Then campaign residuals (vfork/MT-fork-exec), Workstream H, N2 ABI-44 finalize, batch validation, M-SHIP.
Substrate impl: DONE + CONTROLLER-VERIFIED (agent ab7ead30dda0318ce; commits a4fc9599f WIP + e923f9724 final). My independent runs: host-native 40/40 (smoke_fork_externref_reconstructs ok + smoke_fork_gated_externref_parent_survives ok, <1s), fork-codec 431/431, check-abi-version in sync (no drift, no bump). Directly-held externref reconstructs across native fork; call_indirect-minted (provenance-less) externref stays cleanly gated (new native_fork_externref_gate_indirect.wat); gated forks survive (hang fixed). Two extra fixes the agent needed + documented: host-side Table::grow of the anyref transit before decode; gate-hang fix must skip only fm_build_gc_plan/fm_drive_execute (NOT fm_begin_reference_replay).
=== SUBSTRATE + EXTERNREF (host-imported/direct) COMPLETE on native. Node/browser externref parity + FLOOR-2 GC remain. ===

FLOOR-2 GC provenance: grounding dispatched (the hard whole-program transform — struct=appended-field/array=wrapper-struct, field reindexing under subtyping; un-gate struct/array/i31/static-root + fold in extern.internalize externref). LIKELY SERIOUS-DOUBT SPOT — assess feasibility read-only before implementing; surface to user if a genuine wall.
FLOOR-2 GC feasibility: VERIFIED TRACTABLE (grounding ac66c92f5fbf3bbf6 → doc 2026-09-05-n1-f6-gc-provenance-grounding.md, committed b0f5b9765). Campaign's "hard whole-program transform / field reindexing" framing was WRONG (corrected in campaign plan 46529c036): module_gc_codec.rs reads via struct.get/array.get on UNMODIFIED types + bounded constructor-site provenance; no reindexing, no ABI epoch. CONTROLLER-RAN the un-ignored Node round-trip test module_gc_codec_node.rs::fresh_node_instance_reconstructs_gc_cycle_and_identity = PASS (cyclic struct + externref-via-anyref + grandchild re-fork). So GC capture/replay works end-to-end; gate is host-side.

Native GC capture (struct + i31): DONE (agent a7ca5abd277d772ed; commits 789c08951 WIP + 72655b370 final). smoke_fork_gc_struct_reconstructs + smoke_fork_gc_two_object_cycle both PASS; 40 pre-existing green (42 total, 2 fixture-regen ignored). SETTLING EXPERIMENT (two-object mutual cycle) RECONSTRUCTS via $node_base/$pair supertype-seed bootstrap — no block. GcProvenance = Rooted<AnyRef> identity (linear-scan + ref_eq), mirrors ExternrefProvenance. Fixed a real latent bug (ensure_gc_transit_capacity: gc_claim/gc_i31/gc_broker_encode published to transit but nothing grew the table while gated). No ABI drift. CONTROLLER RE-VERIFY in flight (bun6wlmqh).
  STILL GATED (deferred, genuine bounded sub-problems, NOT walls — sequence next): (a) ARRAY capture — 8-vs-0-byte provenance-scalar-length ambiguity needing a GC-codec-descriptor decode (array RECONSTRUCTION already works via the module + Node test; native capture-side only); (b) STATIC-ROOT capture — net-new (grounding says not a revival). gc_define reentrant-fallback path implemented but not distinguishably exercised by the 2 fixtures.
  Task 4 GC report: task-4-gc-report.md

REMAINING reference-completeness work: (1) native ARRAY capture [next]; (2) native STATIC-ROOT capture; (3) Node/browser parity — revive deleted externref+GC capture TS (74c1c373b^) + un-gate all kinds (module side already Node-validated); (4) docs (fork-reference-support.md: move supported kinds; note any residual); (5) fork-package rebuild if needed. NATIVE reference kinds done so far: funcref, externref(host-imported/direct), GC struct, i31.
Native GC ARRAY capture: DONE (agent a09a1b5b1e84ee867; commits cf5275ea1 WIP + cf397703d final). All array forms (ArrayNew/Default/Fixed/Data/Element) un-gated by threading the GC-codec descriptor (fork_codec::gc_codec::decode_gc_codec) into the provenance scalar-length truncation. 43/43 (RED→GREEN self-verified via revert), CONTROLLER-VERIFIED 43/43 + ABI consistent (bg bxqgtptd3). Fixture covers array.new_fixed + array.new_data (exercises the fix) + mutable array-of-struct-refs. Report: task-5-array-report.md.
Native STATIC-ROOT capture: DONE (agent a9534b1dd5fb9eb58; commits fc93fe281 WIP + d718f7533 final). StaticRootProvenance reverse-index (value→coordinate) at the catalog-mirror harvest + gc_lookup consult; 44/44 (RED→GREEN verified), no ABI drift. static_roots_published>0. Concern: only immutable-global source shape has a native fixture (table-first-slot + elem-segment share the code path, not independently exercised); multi-activation out of scope (matches N1-I5). Report: task-6-staticroot-report.md.

=== NATIVE REFERENCE-COMPLETENESS DONE: funcref + externref(direct/host-imported) + GC struct + i31 + GC array + static-root ALL captured+un-gated across native fork. Every reference kind on the forcing-function host. CONTROLLER clean-verify in flight (busunvbg2). ===

REMAINING for the merged reference-completeness increment:
1. NODE/BROWSER PARITY (cross-host, host/src/*.ts) — un-gate all kinds: externref (bind the T1 __wpk_fork_ref_provenance_externref import to ForkExternrefProcessOwner.registerForWire mint-time + un-gate encodeExternref to consume recorded handle — NOT the deleted unsound reverse-lookup); GC struct/array/i31 (revive deleted capture TS from 74c1c373b^: lookupGcSlot/claimGcSlot/encodeI31/defineGc/provenance); static-root (un-gate; ForkStaticRootCatalog TS was NEVER deleted). Decode-side substrate fix (Phase 0b widen) already in shared fork_module32.wasm → propagates to Node/browser on module rebuild (confirm). Node-Vitest validates; browser Playwright batched to campaign end. GROUND THIS FIRST (read-only) — per-kind TS work + module-propagation + test story.
2. docs (fork-reference-support.md: move all kinds to Supported; note native residuals).
3. fork-package rebuild for the F5/T1 provenance-import ABI surface (externref production wrapper).
Then campaign residuals (vfork audit-first, non-main-thread fork/execve, MT-execve teardown), Workstream H, N2 ABI-44 finalize, single cross-host batch validation, M-SHIP curate→merge.
NODE/BROWSER PARITY: DONE (agent ad31a8f9c5863e464; commits 283b06917 impl + 7cf336968 tests+docs). Un-gated externref/GC(struct/array/i31)/static-root on Node+browser mirroring native's GC_LOOKUP layering (dedup→static-root→externref-provenance→GC_CLAIM miss). Revived deleted GC/static-root TS from 74c1c373b^ (+ an intervening commit had also stubbed decode-side GC imports — restored). P0 FIXED (bound __wpk_fork_ref_provenance_externref; instrumented guests no longer crash on Node/browser). Agent: Node Vitest 37/37 across 8 fork suites incl. 3 previously-skipped flag-on tests; M8 module-abort-replay note confirmed STALE (fork succeeds, abort path unexercised); no ABI drift (ABI 44, only a named TS export for the already-declared import). docs/fork-reference-support.md rewritten (199 lines). CONTROLLER RE-VERIFY in flight (bq7gs5aav: 5 inverted fork suites). Report: task-7-nodebrowser-report.md.

=== REFERENCE-COMPLETENESS DONE ON ALL HOSTS: every reference kind (funcref/externref/GC struct/array/i31/static-root) captures+reconstructs across fork on native (44/44 verified) + Node/browser (37/37 Vitest). The campaign's core "all types, no exclusions" goal — achieved cross-host. Browser Playwright batched to campaign end. ===

OPEN ITEMS for batch-validation/ship (NOT reference-logic blockers):
- fork_module32/64.wasm is a BUILT artifact (build-wasm.sh → gitignored local-binaries/), NOT committed + NOT auto-rebuilt in the pipeline. The Phase 0b decode fix (a4fc9599f) lives in fork-codec → the module must be rebuilt for a fresh/CI/browser build or directly-held externref regresses. Wire module rebuild into the build pipeline (or commit the artifact) before batch validation + M-SHIP.
- 8 pre-existing Node Vitest files fail on STALE PACKAGE BINARIES (predate this session) + one missing-rootfs; unrelated to reference work but must be resolved (rebuild) before the clean batch validation. Likely same root as the fork_module freshness item.
- fork-package rebuild for the F5 __wpk_fork_ref_provenance_externref instrumenter ABI surface (externref production wrapper) — census 0/113 use externref-across-fork so no behavior change, but the declared-import surface changed → rebuild for consistency.

NEXT PHASE (residuals; grounding done = d7adc255a): vfork (port proven vfork-lifetime.ts state machine to native Rust + shared-mem plumbing in handle_fork; independent, moderate) → non-main-thread fork/execve (net-new) + MT-execve compute-bound sibling teardown (needs epoch interruption; riskiest; coupled with execve-from-thread). Then Workstream H, N2 ABI-44 finalize, module/package rebuild + freshness, single cross-host batch validation, M-SHIP.
WHOLE-INCREMENT REVIEW (opus, agent a4b71eeab438e57af; doc increment-review.md): SOUNDNESS CORE CLEAN (no unsound reverse-lookup, guards complete, Rooted/transaction ok, Phase-0b safe, gate-hang correct, cross-host wire consistent, ABI structurally consistent). 5 findings.
REVIEW FIXES: DONE (agent a3ac5e78d3e78bb81; commits 01a64a215 #3 + 86bb3e6c5 #2 + 3afe3e619 #1/#4/#5). host-native 45/45, fork-codec 431/431, no ABI drift.
  - #3 (parent-state corruption on gated fork) FIXED: gated_placeholder preserves live value + mints fresh node; test strengthened.
  - #2 (silent array scalar corruption) FIXED: provenance_length_mismatches cross-check → clean gate on mismatch; unit test vs real gc-codec fixture.
  - #1 (provenance-registry OwnedRooted leak + O(n²)) BOUNDED: wasmtime 48 has NO weak-GC-ref primitive (confirmed) → 4096-entry cap + loud diagnostic (truthful boundary) + documented residual vs TS WeakMap. Census 0/113 justifies.
  - #4 (duck-type provenance) documented; #5 (dual ABI-44 import surface) deferred to package rebuild.
  RESIDUAL NOTES (low-impact, for final curation/review): #3 fresh-placeholder-id uses a synthetic i31 counter (theoretical collision on the GATED path, graph discarded unread); #1 cap=4096 is a judgment call.

=== REFERENCE-COMPLETENESS FULLY CLOSED: done → cross-host verified (native 45/45 + Node 10/10 fork suites) → adversarially reviewed (soundness core clean) → findings fixed. All reference kinds capture+reconstruct across fork on all hosts. CONTROLLER final-verify in flight (bb3rttx12). ===

NOW OPENING RESIDUALS PHASE (grounding d7adc255a). Order: vfork FIRST (independent; port proven vfork-lifetime.ts state machine → native Rust coordinator + shared-mem-handle/private-prefix plumbing in handle_fork; reuse kernel vfork_child authority + fm_begin_borrowed_child_replay; moderate, risk medium=shared-mem concurrency). Then non-main-thread fork/execve + MT-execve compute-bound sibling teardown (coupled; net-new; teardown needs epoch interruption = riskiest). vfork edits guest.rs → sequential vs any other guest.rs work.
RESIDUALS PHASE PROGRESS:
- vfork: DONE (commits 1d9825e50/bc8ab8141; 47/47; borrowed SharedMemory + real parent suspension; RED→GREEN verified). Doc'd residuals: refs-across-borrowed-vfork out of scope; no nuclear-teardown for mid-borrow crash.
- fork-from-thread #4a: PARTIAL (commits 96980537c/8b1800bc3; 48/48). NON-INSTRUMENTED guests FIXED end-to-end (pthread fork was a silent OS-thread kill → now returns+reaps+joins; BONUS fixed kernel_exit never wired on worker Stores). INSTRUMENTED guests = DOCUMENTED RESIDUAL (#[ignore]'d smoke_fork_from_thread + full root-cause): parent replay traps in fork-codec RewindDriver::resume_peek/ResumeSlotTable::slot_for for a wpk_fork_resume_thread-reached (non-_start) chain — the host resume table (GuestForkFormat) doesn't cover that slot. Cross-crate (fork-codec/fork-instrument) + MAYBE platform-wide (Node/browser resume_thread not re-verified for pthread-captured fork). High blast radius (replay core) for an edge case → post-ship follow-up. Also noted: nested pthread_create/kernel_clone from a worker thread is the same unwired-import shape (not in scope).
- #4b execve-from-thread + #5 MT-execve compute-bound sibling teardown: DOCUMENTED RESIDUALS (truthful -ENOSYS today; #5 needs engine-wide epoch interruption w/ unresolved safety + unmeasured perf → named scratch experiment as post-ship follow-up). Surfaced to user; proceeding under document-as-residual unless redirected.

RESIDUALS TRUTHFUL-BOUNDARY SET (post-ship follow-ups, all honestly-failing today, none hidden): (a) instrumented fork-from-thread resume-dispatch; (b) execve-from-thread + MT compute-bound sibling teardown (epoch interruption); (c) concurrent fork() from 2 threads of one process (shared fork-module region); (d) nested pthread_create from a worker thread.

WORKSTREAM H (host-surface min; grounding be3a207ac / doc 2026-09-05-n1-workstream-h-grounding.md):
- H1 debug_log parity + H2 is_thread_worker: DONE (agent a4ddc354d8eeb720e; final b184d37b3, recovered from an uncommitted/orphaned-full-rebuild interruption via WIP-commit + targeted `xtask bootstrap kernel`). H1: raw bytes→stderr on ALL hosts (dropped Node/browser [KERNEL] prefix). H2: host_is_thread_worker REMOVED from kernel.wasm (byte-verified), kernel derives internally. HOST-IMPORT SURFACE 84→83 (EXPECTED_HOST_IMPORT_COUNT pin). host-native 48/48, workspace green, Node Vitest suites green (multi-worker 5→0 post-rebuild). ABI: kernel host-imports aren't in the structural snapshot (abi-versioning.md:88) so no snapshot change — surface reduction folded into in-dev ABI 44 (tracked manually). check-abi in sync. CONTROLLER quick re-verify in flight (bap9h8g8m).
  PRE-EXISTING (A/B-kernel-swap-confirmed, NOT H1/H2): 4 Node Vitest fails (process-wait-lifecycle x2, pthread x2) → same root as stale-binary/build-freshness item.
- H3 host_last_errno / wpk_fork_host.* seam: NEXT — confirm-dead-then-DELETE (6 imports; fork-module/src/host_capabilities.rs + native fork_host_capabilities.rs + the NativeForkHostCapabilities impl I built in I5 T2, which F5/F6 bypassed). Biggest host-surface reduction.
- H3 dead wpk_fork_host.* seam: DONE (agent afb814ce17262aefa; commit 7003ab575). Deleted 6 host imports + ForkHostCapabilities/ForkLifecycleCapabilities trait (in fork-codec, not fork-module) + native_sketch.rs backend + hostCapabilities plumbing (~1150 lines). fork-module wasm rebuilt (no wpk_fork_host.* imports). CONTROLLER-VERIFIED: host-native 45/45 (48→45 = deleted seam's tests), fork-codec 430/430, 14 Node fork-module suites 36/36, no ABI drift (these 6 were never kernel-ABI-tracked; EXPECTED_HOST_IMPORT_COUNT stays 83). MORE pre-existing fails found in a 64-file sweep (stale fork-instrumented fixture + a vfork-teardown-timing issue — CHECK the latter at batch validation) + sweep hit resource exhaustion.
- H4 net ioctls → Rust: DONE (agent a44ebd043a304bf96; commit 077bd1b46). SIOCGIFNAME/HWADDR/ADDR/INDEX fully in-kernel (runtime-core/netif.rs via generic ioctl_contract); SIOCGIFCONF content via new kernel_network_ifconf_size/write exports (thin host pointer-chase for the nested ifc_buf, same irreducible reason as msghdr). Host REMOVED: VIRTUAL_INTERFACES, crypto.getRandomValues MAC gen, 4 handleIoctlIf* methods. Added 1 irreducible host import host_network_local_address (real assigned IPv4) → EXPECTED_HOST_IMPORT_COUNT 83→84 (net logic-surface still shrank). ifhwaddr.test.ts RED→GREEN; host-native 45/45 (CONTROLLER-VERIFIED); check-abi additive/no-drift.
=== WORKSTREAM H COMPLETE (H1/H2/H3/H4). H5 /dev/shm+MemoryFileSystem = DEFER (large, own grounding). H-def async park = PARKED. ===

INFRA-HEALTH CLARIFICATION (important): the H4 agent reported 391 runtime-core test-compile errors + wasm-posix-shared won't-build + xtask bootstrap kernel broken (Nix/zstd/lzma). CONTROLLER-PROBED via dev-shell: runtime-core --tests = 0 errors, wasm-posix-shared --tests = 0 errors. So those were ENV-SPECIFIC to non-dev-shell cargo invocation, NOT real breakage. The canonical dev-shell path is HEALTHY. (xtask bootstrap kernel toolchain issue: likely same env cause; the direct cargo build / build-kernel.sh path works — confirm xtask-via-dev-shell during batch-validation setup.)

BATCH VALIDATION (in progress):
- N2 ABI-44: CONFIRMED FINAL. ABI_VERSION=44, snapshot abi_version=44, check-abi consistent (reflects F5 provenance import, H2 removal, H3 seam delete, H4 host_network_local_address).
- Fresh full build (scripts/dev-shell.sh ./run.sh setup, bg bwvh1fw9q, exit 0): kernel.wasm + fork_module{32,64}.wasm + ~60 packages FRESH + succeeded. BUT outcome=failed on 4 packages → DIAGNOSED (bg bcdrjeokr full output) as NON-CAMPAIGN: gzip/tar/wget = transient network `curl (22) 504/502` fetching upstream source; vim = pre-existing compile error (objects/autocmd.o Error 1, BEFORE its fork-instrument step). NONE are fork-instrument/provenance failures — my F5 change did NOT break package builds (confirmed by error text). These 4 block only the BROWSER PRODUCTS (browser-main-shell/wordpress/etc.); core campaign validation unaffected.
- Test artifacts: build-programs.sh + fetch-binaries.sh (bg boywux456).
- Node Vitest FULL (bare npx vitest run, bg b04j0fq6s): 332 files/4082 tests PASSED; 83 files/120 tests failed. CAMPAIGN-CORE SUITES ALL PASSED even under load: externref-fork-module-worker, multi-worker, spawn-{blob,credential,pid,host-parity}, clone-tid-authority, vfork-lifetime, kernel-clone-exit-entry, exec-state-tracking, kernel-authority-boundary, "pthread fork child can fork again", "concurrent pthread forks isolated (flag on)", "mode-1 vfork through fork-module (flag on)".
- FAILURE TRIAGE:
  * fork-instrument-coverage.test.ts (41-42 "fail"): CONFIRMED 5s-TEST-TIMEOUT ARTIFACT, not regression. P-10 (deep linked continuation) PASSES with --testTimeout=120000, taking ~9.3s (assertions green: PRE_DEEP_FORK/DEEP_CHILD:ok/DEEP_PARENT). The heavy deep-fork fixtures exceed the 5s default on this build-loaded machine. Continuation replay (campaign-touched fork-codec) WORKS.
  * Package tests (xz/zip/unzip/git/nginx/php/bash/coreutils/... + "0 test" collection + package-system source-rootfs/resolve-binary): env-blocked by the 4 failed packages (gzip/tar/vim/wget = network 504/vim-compile) + missing downstream artifacts. NOT campaign.
  * Remaining host/kernel failers (abi-version, secure-exec, wasi-shim, exec, login, process-wait-lifecycle, kernel-scratch-*, vfork-lifecycle-guest, sigpending): re-running isolated w/ 120s timeout (bg bxijqmbyd) to confirm timeout-artifact vs real.
- Interpretation so far: NO campaign regression found; failures = tight-timeout-under-load + env-blocked packages. Confirming with the isolated batch.
- ISOLATED (120s timeout): 18 REAL assertion failures across 9 files (not timeouts). A/B vs main (~/src/kandelo):
  * PRE-EXISTING (fail on main too, even the stale Aug-26 kernel): sigpending (1), wasi-shim (3), kernel-scratch-runtime (2). NOT my regression.
  * kernel-scratch-contract (3): static review-GATES my H3/H4 kernel-worker refactor + netif exports tripped → update allow-lists/symbol-refs (my task; not runtime bug; verify kernel_spawn_blob_decode pointer contract).
  * exec (1, prepareExecTargetTestRootfs unreachable), login (2), vfork-lifecycle-guest (1): code paths my session didn't touch → likely pre-existing/env (fixtures/rootfs); confirm if needed.
  * process-wait-lifecycle (2) + secure-exec (3, exit-34): PASSED on the STALE Aug-26 main kernel but that predates main's "interrupted waits" + "credentials/set-ID execution authoritative" commits → CONFOUNDED. Re-running on a FRESH current-main kernel (bg bb3l3b2nv) to decide pre-existing (main's recent set-ID/wait work) vs my regression. [secure-exec exit-34 was noted "deferred" in phase6 memory — consistent with a main-side, not campaign, origin.]
- WEB APP: blocked ONLY by transient network 504/502 fetching gzip/tar/wget upstream source (vim builds fine now — was flaky). Not campaign. Unblock = network recovery OR reuse main's built gzip/tar/wget (campaign-invariant).

=== DEFINITIVE A/B (fresh current-main kernel at ~/src/kandelo, HEAD 888e628d5) ===
PRE-EXISTING on main (fail identically on current main → NOT campaign regressions): sigpending(1), wasi-shim(3), kernel-scratch-runtime(2) = 6 tests.
REAL CAMPAIGN REGRESSIONS (PASS on current main, FAIL on branch): process-wait-lifecycle(2: "wasm32/wasm64 clears and rejects unknown request identity bits") + secure-exec(3: exit-34, "keeps ordinary image outside secure startup" / "nosuid set-ID" / "posix_spawn resets IDs") = 5 tests. MUST FIX before ship. [secure-exec exit-34 = the phase6-memory "deferred" item — confirm w/ user whether intentionally deferred or fix now.]
kernel-scratch-contract(3): review-GATES my H3/H4 changes tripped → update allow-lists (my task).
TBD (not yet A/B'd; code paths I didn't touch this session; likely env/fixture): exec(1, prepareExecTargetTestRootfs unreachable), login(2), vfork-lifecycle-guest(1).
CORRECTION: my earlier "no regression found" was premature — leaned on not-touched-code-path heuristic + a STALE Aug-26 baseline. The clean fresh-main A/B (user-directed) revealed the process-wait + secure-exec regressions.

REGRESSION FIXES (agent ae1ff8a84d9c2f8d4; user said fix both now):
- process-wait: FIXED (commit 092fae316). Root: Phase-2 opaque-transport took REQUEST_FLAG_OPAQUE_RECORD=8, colliding with the test's literal "unknown bit" 8 → a real flag rode a RAW syscall (Wait4) into a fatal assert. Fix (host/src/kernel-worker.ts #captureChannelRequest): reject OPAQUE_RECORD on HOST_RAW_SYSCALLS (EINVAL). 69/69 green.
- secure-exec exit-34: FIXED (commit 6fbb939ec). Root (from 7662088ef in-kernel tmpfs cutover): (1) test staged /tmp fixtures via a host mount the cutover made unreachable (kernel owns /tmp) → probe self-seeds /tmp via guest write(); (2) real kernel-worker.ts bug: read-only mmap(PROT_READ,MAP_SHARED) of a kernel-owned tmpfs file wrongly ENOTSUP'd → fixed to only ENOTSUP for WRITABLE. 17/17 green. BOTH fixes are HOST-TS (kernel-worker.ts), NOT kernel-wasm.
- THIRD REGRESSION FOUND (NOT fixed, needs decision): host/test/mmap-shared.test.ts — WRITABLE MAP_SHARED at /tmp. Genuinely unimplemented (needs host↔kernel writeback bridge). Same 7662088ef tmpfs-cutover origin; passes on main. This is the cross-process-shared-writable-mmap case (relates to [[cross-process-shared-memory-vs-opcache]] limitation). DECISION PENDING: implement the writeback bridge vs document as a known gap.
Fix-agent report: task-14-regression-fixes-report.md. TODO: controller re-verify the 2 fixes (green + still pass on main).

WEB APP BUILD+SERVE (env/build-infra fight):
- gzip/tar/wget: 504'd on ftpmirror.gnu.org → fetched from direct ftp.gnu.org (sha-verified vs package.toml) + placed in packages/registry/<pkg>/<pkg>-src (gitignored) → build skips the fetch. vim flaky (builds now).
- OOM (137): ./run.sh browser (and jobs=1 retry) OOM-killed while BUNDLING the heavy DB products (nginx-php/wordpress/lamp) — machine memory-starved (WebKit+Brave+Conductor+agents+louisville-v1's concurrent spidermonkey-node build). Shell demo (browser-main-shell) BUILT fine; DB demos are the hogs. WASM_POSIX_LOCAL_BUILD_JOBS controls parallelism.
- SERVE path: cmd_browser = cmd_prepare_browser (=cmd_local_build --product all) + `npx vite` in apps/browser-demos reading the source-only projection (WASM_POSIX_SOURCE_ONLY_BINARY_ROOT=local-binaries/source-only-v1). Custom subset: xtask local-build run --set packages/sets/local-supported.toml --source-cache-root ~/.cache/kandelo/source-only --output-root <ABS>/local-binaries/source-only-v1 --product browser-main-shell [--rebuild] --jobs 1. (--output-root MUST be absolute.)
- *** REAL FINDING — STALE SOURCE-ONLY KERNEL CACHE (ship-relevant): the source-only kernel PACKAGE cache did NOT invalidate on my H1/H2/H4 kernel+runtime-core edits → source-only projection records kernel size 998931 (pre-change) while my real kernel (local-binaries/kernel.wasm, used by Vitest/conformance) is 1003144. vite rejects the member-size mismatch. Same class as #1328 "kernel build cache-key omits runtime-core". My out-of-band kernel builds (xtask bootstrap / direct cargo) masked it for Vitest but the source-only/browser path serves stale. FIX for browser: --rebuild the source-only kernel (bg bdk21szx7). SHIP TODO: the kernel package build.toml `inputs` must cover ALL my changed kernel/runtime-core files (netif.rs is NEW; wasm_api.rs/syscalls.rs/process.rs changed) or source-only consumers get a stale kernel. Verify + fix the cache-key inputs.
- REMAINING: cargo workspace + xtask; libc/posix/sortix conformance; browser Playwright (product-dependent = env-blocked by vim/network, run runnable + document).
=== was: N2 confirm ABI-44 final (check-abi already consistent; confirm ABI_VERSION=44 + snapshot reflects all campaign surface changes: F5 provenance import, H2 removal, H3 seam deletion, H4 host_network_local_address) → BUILD-FRESHNESS (fresh full build via dev-shell/./run.sh setup — rebuild kernel + fork_module + packages; confirm fork_module rebuild wired; triage pre-existing Node Vitest fails [process-wait-lifecycle, pthread, stale fork-instrumented fixture, vfork-teardown-timing] = stale-artifact vs real) → single cross-host BATCH VALIDATION (Node Vitest full + browser Playwright + libc/posix/sortix) → M-SHIP (curate self-contained commits, preserve attribution; HAND MERGE TO USER — do NOT merge autonomously).

THEN (ship path): finish H (H3, H4) → N2 ABI-44 finalize → module/package build-freshness (fork_module rebuild in pipeline + stale-binary rebuild; subsumes the pre-existing Node failures) → single cross-host BATCH VALIDATION (Node Vitest + browser Playwright + libc/posix/sortix) → M-SHIP (curate self-contained commits, preserve attribution, rebase-merge for user).

## 2026-09-06 — Build-freshness DONE + fresh web app delivered
- Build-freshness fix committed: 2cd223397 (skip-path content verification — the real fix, NOT a cache-key input gap), d897f7027 (fork_module closure freshness stamp + generalization guard), 51c1e4891 (docs). cargo test -p xtask 598/598 bar 1 pre-existing unrelated ABI-count. fork-instrument→guest re-instrument empirically confirmed (sqlite-cli rebuilt on externref_provenance.rs touch). Root cause: source_only_skip_receipt_if_clean trusted a Cached disposition when a file merely EXISTED at the projection path (same-size stale bytes evaded it) — now re-hashes vs receipt sha256/size. Reverted the probe perturbation; tree clean.
- Branch fully current with main (merge-base with 888e628d5 == 888e628d5; nothing to pull; already contains #1330 dep-closure guard + #1328 runtime-core cache-key).
- FRESH FULL BUILD: ./run.sh prepare-browser (xtask local-build --set local-supported --product all) → 98/98 nodes, 97 cache hits, 1 built, 7/7 products (WordPress built this time, no OOM). Projection kernel.wasm=1003144 (fresh, produced by the freshness-fixed engine). verify-fresh (ABI) exit 0.
- SERVING: vite on http://localhost:5455/ (node pid 14158, detached nohup). Headless smoke test GREEN: shell demo boots to live -bash-5.2$, 0 console/page/request errors, 95 wasm/worker reqs. Screenshot /tmp/kandelo-smoke.png.
- KNOWN NON-BLOCKING SEAM: vite dep-scan errors on fork_module32.wasm "not owned by pinned SourceOnly projection" (Phase 6 D5 host import; built out-of-band by build-wasm.sh into local-binaries/, not projected). Non-fatal (default boot never resolves it). See memory fork-module-not-projected-seam. Proper fix = project it like kernel.wasm; deferred (touches projection contract + delivered app).
- NEXT (autonomous, safe — no browser-projection impact): third regression host/test/mmap-shared.test.ts (writable MAP_SHARED writeback bridge). Then M-SHIP curation.

## 2026-09-06 (cont) — mmap-shared regression FIXED; all 3 regressions closed
- mmap-shared writable MAP_SHARED writeback: commits 13c96774a (host fd-pread/pwrite writeback bridge in kernel-worker.ts + test), 2cc0407a2 (memfd/tmpfs unit-test update). Root cause: tmpfs cutover 7662088ef → kernel-owned /tmp files have hostHandle===null → writable-MAP_SHARED (host-owned byte-store keyed by handle) short-circuited to ENOTSUP. Fix = host-only bridge: initial load SYS_PREAD, writeback SYS_PWRITE at existing publication points (msync/munmap/exec+process teardown). NO kernel change, NO ABI bump (check-abi-version green). Ruling: memfd vs tmpfs indistinguishable to host → both get bridge; worst case truthful EIO. Validation: branch mmap-shared PASS 9.5s (added 30s it() timeout — heavier kernel ~9s e2e, not an mmap slowdown), adjacent mmap-tracking/shared-memory-coherence/file-shared-memory 71/71 PASS, main baseline mmap-shared PASS re-confirmed. (mmap-file.test.ts env-fail: needs rootfs.vfs, intentionally not built to protect served app.)
- ALL 3 REGRESSIONS CLOSED: process-wait 092fae316, secure-exec 6fbb939ec, mmap-shared 13c96774a/2cc0407a2.
- Session commit set on top of main 888e628d5 (this session): 092fae316, 6fbb939ec, 2cd223397, d897f7027, 51c1e4891, 13c96774a, 2cc0407a2. Tree clean; vite :5455 up.
- NEXT: focused adversarial review of these 7 commits (pre-merge), then user-driven M-SHIP curation + rebase-merge (user = sole merger; nothing pushed). Deferred: fork_module projection seam (with user).

## 2026-09-06 (cont) — mmap fix-round 1 DONE (review findings resolved) + re-review dispatched
- Adversarial review (task-17) of 7 session commits: FIX-FIRST, 1H/2M/4L, all in the mmap bridge; other 6 sound.
- Fix round 1 (resumed mmap implementer): commits 5f455e156 (H1 no-grow clamp via live fstat + M2 F_DUPFD_CLOEXEC stable dup surviving close, refcounted + M3 fork inheritance + dirty-run diff coherence + L4 O_WRONLY EACCES), b1ea0df4a (L7 loud [ABI DRIFT] diagnostic + L6 closure-guard de-noise), 2e730d6ee (test timeout 60s). No ABI bump (check-abi-version green). Ruling: rejected full SharedMmapBacking unification (would need new kernel exports = ABI bump, out of scope unsupervised) — mirrored semantics host-only. L5 (wire fork-module verify-fresh into CI gate) noted optional/not-done. Validation: adjacent suites 77/77, mmap-shared real-kernel H1/M2 green, file-shared-memory 53/53, cargo_closure 7/7, main baseline green, tsc clean.
- Served app re-verified after kernel-worker.ts change: shell demo boots -bash-5.2$, 0 errors (smoke test). vite :5455 up.
- Scoped re-review dispatched over 2cc0407a2..HEAD (verify fixes correct + no new defects in fd-lifecycle/coherence).

## 2026-09-06 (cont) — mmap re-review + round-2 DONE; mmap work COMPLETE
- Scoped re-review (task-18): all 6 round-1 findings CONFIRMED RESOLVED; 2 new (NEW-1 Low exec refcount-map leak, NEW-2 Medium guest-visible-dup close/dup2 hazard).
- Fix round 2: commit e6e269960 — NEW-1 (delete fdWritebackFdRefs[pid] in exec finalize) + NEW-2 (flushFdWritebackMapping re-fstats dup, verifies (dev,ino) before pwrite → closed dup refuses+reports, repointed dup refuses (no cross-file corruption), truthful EIO + [MAP_SHARED writeback lost] diagnostic instead of silent loss; boundary documented; full guest-invisible-kernel-handle fix deferred as ABI work). Host-only, no ABI bump. Validation: branch serial 80/80, file-shared-memory 56/56, main baseline green, check-abi-version green, tsc clean.
- Served app re-verified after this change: shell demo boots, 0 errors. vite :5455 up.
- MMAP WORK COMPLETE. All 3 regressions closed + reviewed + hardened. Session commits on main 888e628d5: 092fae316, 6fbb939ec, 2cd223397, d897f7027, 51c1e4891, 13c96774a, 2cc0407a2, 5f455e156, b1ea0df4a, 2e730d6ee, e6e269960.
- REMAINING (for user): M-SHIP commit curation + rebase-merge (user=sole merger; nothing pushed). Deferred non-blocking: fork_module projection seam (browser dep-scan noise); L5 (fork-module verify-fresh CI gate); NEW-2 full kernel-handle fix (ABI).

## 2026-09-07 — Path-B execution (P1/P0/P2 done; P3 split)
- Plan-of-record: docs/plans/2026-09-06-path-b-flip-completion.md (three-bucket map: reconstruction engine already shared Rust; work = V8 wiring+deletion + one capture-export). Native = reference architecture.
- P1 DONE d372dd4c3 (native reconstructs all kinds via shared module; stale gate comments fixed; host-native 45/45).
- P0 DONE 020770377 (fork_module32/64.wasm now SourceOnly projection-owned members like kernel.wasm; freshness via closure stamp + re-staging gate; resolver not weakened; dep-scan seam closed). [Recovered after I wrongly stopped it mid-54-test run.]
- P2 DONE 6467a11df + fix 4f0d67972 (module = SOLE V8 reconstructor; all-or-nothing gate + JS reconstruction fallback removed; materializeAllTyped runs full module drive). Review (task-P2-review) FIX-FIRST 1 MEDIUM: restored fail-loud exnref tag-validity boundary (host-side, staying fork-module-exnref-admission.ts, EINVAL, tested 3/3). Phase 0b externref crux verified correct in Rust.
- P3 SPLIT. P3a DONE: ba4e20ee5 (fm_capture_* scalar exports surfacing ReferenceGraphBuilder + push_gated_placeholder; no ABI bump), 4ff89a5bf (V8 capture proof harness + --release null-base memory-read bugfix). fork-codec 431, 3 V8 harnesses green, verify-fresh green. Design (a) mirror-native (capture layering = host floor). P3b REMAINS (BLOCKED-not-forced): flip worker-main ENCODE imports + delete JS capture requires re-sourcing the PARENT's own post-fork replay reads (getReferenceVector/routeGc/loadGc) from the module while handing back original live values — the capture↔replay symmetry seam across ForkReferenceTransaction+ForkContinuation+worker-main; needs native+Node parity. Concrete unblock in task-P3-report.md (mirror native parent-replay vector sourcing).
- NEXT: P3b (parent-replay re-source + encode flip + JS-capture deletion, cross-host parity) → adversarial review → P4 gated-abort parity → P5 default-ON flip (user checkpoint) → P6 delete JS twins → P7 batch validation.

## 2026-09-07 (cont) — P3 FULLY DONE (hardest seam crossed)
- P3a: ba4e20ee5 (fm_capture_* scalar exports) + 4ff89a5bf (V8 capture harness + --release null-base memory-read bugfix).
- WIP preservation: 52ba17d1b (I committed the agent's in-progress P3b after wrongly stopping it twice on transcript-idle — see memory subagent-transcript-idle-not-stuck) + 9671acd3a (plan doc).
- P3b: 0501e192a (core capture flip + parent-replay re-source via fm_capture_vector_get — identity by construction), 3ed840952 (exnref capture), 5df61130a (host-exception capture). Module is SOLE V8 capture graph for all kinds. Load-bearing fix: moved module bump-heap reset from fm_begin_unwind → fm_capture_begin (guest encodes refs before AND after unwind). JS capture PHYSICAL deletion deferred to P5/P6 (still the flag-off path + peer-table snapshot). host-native 45/45 + Node module-on suites green (32/32 + 18/18 abort + 50/1 sweep + 47/47 continuation + fork-codec 431), no ABI bump.
- P3b review: SHIP. Parent live-reference identity CONFIRMED INTACT by construction; bump-heap move sound. 5 Low findings.
- P3b hardening: 9eeb84947 (A: recordModuleCapturedValue fails loud on id/length mismatch +test; B: routeGc/gcPayloadLength guarded against module-parent-replay; C: stale comment). host-native 45/45, Node module-on 23/23, ABI clean, fork_module re-projected.
- NEXT: P4 (module-mode gated-abort parity, review finding D) → P5 default-ON flip (USER CHECKPOINT) → P6 delete JS twins → P7 batch validation.

## 2026-09-07 (cont) — THE FLIP LANDED (P5) + P5b
- P4 DONE 4df57c39d/2bcb79c0f (module-mode gated externref abort clean on Node+native, bounded time, module-driven proof; +fixed a frame-emit vs kernel_exit race).
- P5 DONE 9e0593416 (flip: Node WASM_POSIX_FORK_MODULE default ON via !=="0" kill-switch + tri-state host fix; browser main-thread ships module+flag, worker-entry default true honest) + 4344ec2f8 (reconcile 5 flag-off tests that FAILED because the module now runs by default = direct proof of the flip). Default-config Node Vitest 36 pass/1 skip, gate fails loud. No ABI bump. MODULE IS NOW THE DEFAULT FORK ENGINE ON V8.
- P5b DONE 45488f08b (full fix, not a gate): exception-carried reference payloads reconstruct on the module default (readModuleRecipeIds sources from module; append_vector EINVAL is authority = still fails loud). JS-pinned test un-pinned to module path. host-TS only, no ABI. Node-validated (exnref proof-of-use, child exit 0, gate green). Unblocks P6.
- CONCERN to resolve before P7: P5b could not RUN host-native (Nix env: cc-wrapper -fzero-call-used-regs=used-gpr + missing wasm sysroot breaks wasm32 C build-deps zstd-sys/wasmtime-internal-jit-debug). BUT P4/P3b ran host-native 45/45 green in this worktree recently → likely transient/local. MUST verify native builds+tests before P7.
- NEXT: P6 (delete ~12.8k dead JS twins; keep floor identity primitive + fork-module-*.ts glue + fork-reference-unsupported + fork-module-exnref-admission + peer-table captureTableState/restoreTableState path) → verify host-native → P7 cross-host batch validation (incl. browser boot for user dogfood).

## 2026-09-07 (cont) — ORCHESTRATION MIGRATION (re-scoped L-scale; JS still orchestrator)
- Verified (task-orchestration + verify-pass): at HEAD the module owns only LEAF drives; JS ForkActivationRegistry/ForkReferenceTransaction is still the live orchestrator/graph-owner on the module-on path. P2/P3 "no JS path" only measured the leaf algorithm, not the orchestration wrapper (measurement gap). materializeTypedGraph is LIVE (imported-global i31/struct/array). Corrected plan: docs/plans/2026-09-07-fork-orchestration-migration.md. Honest scope = 5 new module exports + L-scale host-container rewire (fork-process-continuation.ts + worker-main.ts written entirely against JS registry API, dozens of unconditional call sites) + first-ever cross-host module-on validation. NOT a wiring-and-delete finish.
- Exports increment (1 of migration): 3/5 landed + isolate-validated — a18aa890f (fm_decode_reference_graph + fm_scan_externref_handles), 9a749dacc (fm_restore_from_arena = seed+drive-plan in one call, byte-identical to two-step). One rebuild, no ABI bump (additive over frozen wire), 15/15 replay green. BLOCKED (correctly, guest-instance-coupled → do in host-rewire): fm_attach_child, fm_attach_borrowed_child, imported-globals seeding.
- FOOTGUN flagged: build-wasm.sh does NOT sync local-binaries/source-only-v1/ (which resolveBinary prefers) → fresh fork_module silently shadowed. Must rebuild via local-build OR sync manually. (P0 projected it, but the standalone build-wasm.sh path bypasses the sync.)
- NEXT: host-container rewire increment (author attach_child/attach_borrowed/imported-globals vs live child; re-point fork-process-continuation.ts + worker-main.ts off JS registry onto module entries; detach unconditional JS constructions; assert JS wrapper NOT constructed on default path; cross-host validation) → review → P6 delete → verify host-native → P7.

## 2026-09-07 (cont) — orchestration migration slices landing
- Exports: a18aa890f (fm_decode_reference_graph + fm_scan_externref_handles), 9a749dacc (fm_restore_from_arena). Reconstruction-entry rewire e3d64f852 (attachModuleChild uses fm_restore_from_arena). Attach: 215e96923 (fm_attach_child + fm_attach_borrowed_child = module-owned install SEQUENCING via DRIVE_OP_RESTORE/FINISH_RESTORE; guest does layout placement). RULINGS: imported-global materialization + catalog mirroring = genuine HOST FLOOR (not migrated). fm_drive_bump gated to reconstruction ops (keeps reference-free-fork-silent contract).
- host-native HEALTHY: 45/45 — earlier "native build broken" was invocation (default target hits Nix wasm-dep issue; build on host triple per its Cargo.toml). P7 native leg de-risked.
- All slices: no ABI bump (additive exports over frozen wire), rebuild+source-only-v1 sync confirmed (build-wasm.sh footgun handled by manual re-sync).
- REMAINING: Step 3 capture rewire (route ForkReferenceTransaction graph/catalog ownership to module; separate floor: externref WeakMap identity, catalog host-reads, live-ref layering) → Step 4 detach unconditional JS constructions (ForkActivationRegistry/LinkedForkContinuation/ForkProcessContinuationCoordinator/ForkReferenceTransaction) → assert JS wrapper NOT constructed → P6 delete → P7 cross-host validation (Node+browser+native-on-host-triple).

## 2026-09-07 (cont) — capture assessment: remaining finish is TS-ONLY (zero Rust)
- Capture-rewire agent BLOCKED-but-clarifying: capture GRAPH ownership already fully module-owned by P3b (construction/dedup/cycle/gate/validate/serialize/parent-read all via fm_capture_*). NO Rust/rebuild/ABI left for the finish. ForkReferenceTransaction on module path = floor-only shell + delegate.
- Remaining = pure host-TS re-homing: (1) author staying fork-capture-session.ts owning the floor (funcref/static-root catalog host-reads, externref WeakMap identity, STORE #2 transit publish, live-ref layering lookupGcSlot, capturedValues identity round-trip, dedup maps) + driving fm_capture_*; (2) extract shared scratch/linear-memory infra out of the 2721-line fork-reference-transaction.ts; (3) reroute fork-activation-registry.ts capture(:1485)+reconstruction(:1700) to session/module, delete transaction dead branches; (4) detach unconditional JS constructions → engine files test-only → assert JS-wrapper-gone → P6 delete → P7.
- Corrections: capture-parent vs child-reconstruction = SEPARATE phase-keyed instances (severance cleanly scoped). fork-function-catalog.ts + fork-static-root-catalog.ts = FLOOR (OFF the P6 delete-list). Atomic: capture encode = 24 imports sharing state, no JS fallback at externref-identity seam → must land cohesively + validate in batched cross-host P7.

## 2026-09-07 (cont) — APPROACH CHANGE: detach is a RECONSTRUCTION problem; land as ONE cohesive batch
- Capture-session-detach BLOCKED (6th block at this boundary: P5c/P6/host-rewire/capture-rewire/capture-session + first batch). Root cause: ForkActivationRegistry constructed UNCONDITIONALLY (worker-main.ts:3819); module reconstruction runs THROUGH it — registry.attachChild (:1701) still constructs ForkReferenceTransaction + JS-decodes wire graph; restoreModuleState (:1719) still JS transit-sizing via materializeAllTyped. e3d64f852 rewired the ENTRY (fm_restore_from_arena) but not the CONTAINER. The detach can't land piece-by-piece; it's ATOMIC.
- CHANGE OF APPROACH: stop slicing. Land the WHOLE remaining integration as ONE cohesive host-TS batch (exports all exist → no/minimal new Rust): finish routing the full reconstruction call-chain through fm_decode_reference_graph/fm_restore_from_arena/fm_attach_child (kill in-container JS decode + transit-sizing) + extract capture floor to fork-capture-session.ts + detach the unconditional JS constructions, validated Node+host-native(host-triple) in the SAME increment, with the JS-wrapper-gone assertion. Browser Playwright P7 + P6 delete + review = coordinator follow-up.
- Confirmed floor/keep: fork-function-catalog + fork-static-root-catalog (FLOOR, off delete-list); fork-anyref-transit STORE #2; fork-reference-broker resolve_externref.

## 2026-09-07 (cont) — Path A chosen; grounding done (docs/plans/2026-09-07-path-a-peertable-migration.md)
- Pushed to PR #1350 through 3000e5257 (21 code commits + 2 plan docs, ff). Local fallback tag path-b-branch-point-2026-09-07 @ 215e96923.
- GROUNDING (great news): ZERO new Rust for Path A. The "fm_restore_from_arena opens a fork generation" premise was STALE — M2 host-generation seam retired at HEAD (pid unused, lib.rs:2374), so module reconstruction entries are already generation-free/table-neutral. ~100% of fork-codec capture↔replay + fm_capture_*/fm_restore_from_arena/fm_drive_execute reused as-is for table snapshots. Recommended new module surface = ZERO new fm_* (staying fork-table-snapshot.ts composes existing; optional +1 drive-op = snapshot-check only, no epoch).
- A converges with B: A2 (capture-session floor extraction + fork severance) = the shared hard atomic seam; A adds A3 (table-snapshot glue, zero Rust) + A4 (rewire DylinkForkTableReplica) so files become deletable. Wall-2 = re-home pure-wire scan to staying glue (kernel worker holds only module BYTES, no instance).
- KEY UNLOCK: A2 does NOT need to make files test-only (that was why it blocked 7×) — it extracts floor + drives fork through module; co-tenants A3/A4 then A5-delete. Bounded/landable.
- Sequence: A1 kernel-scan re-home (S/Low, indep) → A2 floor-extract+fork-sever (M-L/HIGH atomic) → A3 table-snapshot glue (M/Med, 0 Rust) → A4 dylink rewire (M/Med-High) → A5 delete → A6 cross-host+dlopen+browser validation. Gotcha: process-table-replication.test.ts MOCKS capture calls → need new real-engine round-trip + dlopen e2e + browser specs.
- NOW: A1 dispatched.

## 2026-09-07 (cont) — Path A executing (batched validation)
- A1 DONE 3c141d5d7: kernel externref scan re-homed to new staying host/src/fork-reference-wire.ts (KFRV v2 read surface + FORK_REFERENCE_TRANSACTION_OWNER_ID from ABI). fork-externref-process-owner.ts no longer imports fork-reference-segments.ts OR fork-reference-transaction.ts. Wall 2 done. fork-reference-recipes.ts NOT forced to stay. typecheck clean, scan/decode 34/34, replay 17/17, host-native 45/45, no ABI. Remaining engine importers = fork cluster + worker-main peer-table (A2/A3/A4/A5).
- VALIDATION POLICY (user): lightweight per-step gates (typecheck + directed suite + commit); DIRECTED suite for the 2 risky steps (A2 fork suites, A4 table-replication/dlopen); ONE heavy cross-host+browser+dlopen battery batched at A6.
- A2 scope UNBLOCK: A2 does NOT need files test-only (that was the 7× blocker) — extract capture-session floor to staying fork-capture-session.ts + drive FORK path fully through module. Peer-table co-tenant stays on the files until A3/A4; A5 deletes.
- NOW: A2 dispatched.

## 2026-09-07 (cont) — A2 partial: grounding's "zero Rust" WRONG; remaining core is Rust-bearing atomic batch
- A2 committed 14ce74fd6 (scratch/memory infra → staying host/src/fork-reference-scratch.ts, behavior-preserving; 15 files/82 tests, host-native 45/45, ABI clean). Prereq for capture-session floor + A5 delete.
- A2 BLOCKED on the two primary deliverables (code-grounded, real Rust needs — 3rd time implementation corrected optimistic analysis):
  * SEVERANCE needs INC-C (Rust): worker-main's decodeSegmentedForkReferenceTransaction produces the decoded STRUCTURE (node kinds/exnref tag ordinals/static-root ordinals) feeding host wiring; fm_decode_reference_graph only exposes node-count+externref-handles → need structure-exposing decode exports. + INC-B (Rust): imported-global reconstruction runs in JS at guest-instantiation time (ForkEarlyChildReferenceProvider) before the module can drive → module-owned imported-global recon. JS recon ALGORITHM already dead (P2 early-return); removing the OBJECTS needs INC-B/C.
  * CAPTURE-SESSION floor is atomic per-instance + host-exception parent-replay routes through this.nodes (empty in module mode) = unexercised/latently-broken → needs WebKit validation, no JS fallback at the seam.
- Remaining core = atomic, Rust-bearing, cross-host(incl browser)-validated batch; resisted 8 slicing attempts. Plan: author INC-C decode-structure exports (isolate-validate, proven pattern) → INC-B imported-global recon → capture-session floor + severance wire → single cross-host validation (Node+native agents; browser leg via ./run.sh browser).
- NOW: INC-C dispatched.

## 2026-09-07 (cont) — only Rust was INC-C; INC-B was a re-home; surface cleared for the atomic capture-session
- INC-C DONE e3b661881 (fm_decoded_node_kind/module_activation/ordinal, structural-parity proven 3/3, host-native 45/45, no ABI).
- INC-C wiring + INC-B: 20c07aee3 (wire the 2 worker-main consumers to INC-C accessors; worker-main 0 .graph.nodes reads; found+fixed: worker-main never called fm_decode_reference_graph, added one decodeReferenceGraph call serving both, survives instantiation+attach). cfaff94c3 (INC-B VERDICT: imported-global recon is genuine HOST FLOOR not Rust — module can't new WebAssembly.Instance pre-guest; A2's "Rust-bearing" was WRONG; re-homed contracts to staying fork-reference-contracts.ts + FORK_HOST_EXCEPTION_ACTIVATION_ID to fork-reference-wire.ts; fork-early-reference-provider no longer imports the deletable engine). host-native 45/45, no ABI, no Rust.
- ONLY Rust for the whole finish = INC-C (done). Everything else host-TS + batched validation.
- Remaining deletable-engine importers: worker-main.ts (peer-table + fork), fork-process-continuation.ts, fork-exception-provider.ts.
- A2 host-exception concern to handle in capture-session: exceptionOwner/materializeHostException parent-replay routes through this.nodes (empty in module mode) → unexercised/latently-broken; add a host-exception parent-replay test; WebKit confirm at A6.
- A5 residual: fork-early-reference-provider still imports read-only vector-materialization from fork-reference-segments.ts (floor; A5-min = segments stays, fine; A5-full = extract encode-side vector subsystem).
- NEXT: atomic capture-session floor extraction + fork-container severance (Wall A) → A3/A4 peer-table (zero-Rust) → A5 delete → A6 cross-host+dlopen+browser validation.

## 2026-09-07 (cont) — CAPTURE-SESSION WALL CROSSED
- 0fa4de302 capture-session floor extraction + host-exception fix + capture-side severance (host-TS, no Rust). fork-capture-session.ts owns capture floor drives fm_capture_*; fork CAPTURE path constructs no ForkReferenceTransaction. Host-exception parent-replay returns original from capturedValues (test 4/4). fork-exception-provider + fork-process-continuation + worker-main fork path cleared of transaction. Validated: fork gate 20/117, host-native 45/45, typecheck, ABI in sync. Pushed.
- REMAINING new ForkReferenceTransaction: :1548/:1620 (peer-table co-tenant → A3/A4), :1712 (CHILD RECONSTRUCTION object = floor wrapper: holds wire-decoded graph + transit sizing + adoption target — next host-TS severance, decomposed sound-looking no-Rust), :1497 (flag-off fallback → final detach). Then A5 delete + A6 cross-host+dlopen+browser validation.

## 2026-09-07 (cont) — DECISION (user): MAXIMUM JS REMOVAL, NO KILL-SWITCH
- Fully commit to module-only fork reference engine. NO WASM_POSIX_FORK_MODULE / forkModuleEnabledBrowser toggle, NO flag-off JS fallback. The module is the UNCONDITIONAL fork path.
- Reshapes endgame:
  1. Remove the flag/kill-switch entirely (WASM_POSIX_FORK_MODULE default-on kill-switch + forkModuleEnabledBrowser) → module unconditional; remove the flag-off fallback (fork-activation-registry.ts:1497) + any moduleReferenceKindsSupported/useForkModule branching.
  2. A3/A4 peer-table migration is now MANDATORY (no JS fallback — the engine files it uses get deleted).
  3. A5 deletes ALL the JS reference engine files + all flag-off branches; remove/convert the flag-off TEST variants (the flag-on tests become THE tests).
  4. A6 validates module-only (simpler matrix: no flag-off variant).
- Capture-session review = SHIP (parent-identity + atomicity PASS; non-blocking LOW/INFO nits). Child-reconstruction severance in flight (a2c3c95d76c48e576).

## 2026-09-07 (cont) — A2 COMPLETE (module-on fork path fully JS-engine-free)
- Child-reconstruction severance: 778a92a31 (ForkModuleReconstructionFloor replaces the :1712 wrapper: decoded graph→module resident, transit sizing→fm_decoded_node_count, adoption→validate-only; caught+fixed scratch-arena floor role via fork-reference-scratch.ts) + 19e776ea5 (floor unit test 7/7). Directed gate 22 files/125 tests, host-native 45/45, no Rust/ABI. No-transaction proof: module-on attachChild yields the floor, not a transaction.
- Remaining ForkReferenceTransaction: :1549/:1621 peer-table (A3/A4, now MANDATORY per max-JS-removal), :1498 flag-off capture + :1736 flag-off child recon (→ kill-switch removal).
- NEXT: A3/A4 peer-table → module (zero-Rust) → kill-switch/flag removal + flag-off fallbacks → A5 delete → A6 module-only cross-host validation.

## 2026-09-07 (cont) — A3/A4 DONE (peer-table module-only)
- 4b2385146 (fork-table-snapshot.ts + ForkTableReconstruction staying glue; DylinkForkTableReplica rewired; pthread setCaptureModule fix; zero new fm_*, zero Rust) + 5fef8bebf (real-engine round-trip test, replaces mocked coverage). Ruling: peer-table restore runs in a live worker with JS-bound guest decode imports → decode against this-worker catalogs (ordinal stable by construction); host-TS seam not Rust. Validated: roundtrip 1/1, dlopen e2e 7/7+6, table-replication 3/3, fork gate 41/41+27/27, host-native 45/45, ABI green.
- :1549/:1621 CLEARED. ONLY remaining ForkReferenceTransaction = :1502/:1761 (fork flag-off branches).
- A6 note: externref/GC-in-tables implemented but not test-covered (add non-funcref table-entry scenario at A6).
- NEXT: kill-switch + flag-off removal (remove WASM_POSIX_FORK_MODULE/forkModuleEnabledBrowser flags + :1502/:1761 fallbacks + moduleReferenceReplay/useForkModule gating → module UNCONDITIONAL; convert flag-off test variants → module-only) → engine files test-only → A5 delete → A6 module-only cross-host validation.

## 2026-09-07 (cont) — KILL-SWITCH + FLAG-OFF REMOVAL DONE (module unconditional)
- Commits on 5fef8bebf: c256becd5 (remove kill-switch + flag-off JS engine path: WASM_POSIX_FORK_MODULE/forkModuleEnabledBrowser + option/protocol plumbing gone, module always shipped+instantiated, :1502/:1761 → fail-loud), 552b8ee7e (convert flag-off fork test variants → module-only), edc4e985b (re-home JS-engine orchestration unit tests onto the module via new fork-capture-module-fixture.ts; skip in-process/child-recon cases), 79ec2c4b0 (fix dlopen-pthread fork hang the removal exposed).
- PROOF: ZERO `new ForkReferenceTransaction` in non-test code (all 13 constructions are test-only). host-native 45/45, typecheck clean, check-abi green, gated fork fails loud + parent survives, dlopen/vfork/peer-table/reference-kind e2e all module-driven green.
- RULING: useForkModule / moduleReferenceKindsSupported / moduleReferenceReplay are genuine FRAME/RESUME capability + parent/child gates, NOT the kill-switch — kept; a fork the module can't back (resume catalog > cap; instrumented fork-from-thread child w/o alt resume entry) now FAILS LOUD, not silent JS.
- FIX: dropped the `!hasDylinkForkRole` gate on the pthread fork-module block — a dlopen pthread now instantiates the module (else beginCapture threw → 30s hang); A/B-confirmed regression (base 2.1s vs removal hang), fixed.
- A5 CARRY (proof 2): the ForkReferenceTransaction CLASS is orphaned (constructor-dead outside tests), but the delete-list FILES still have non-test importers for NON-engine functionality — fork-reference-transaction.ts (FORK_REFERENCE_TRANSACTION_OWNER_ID + decodeSegmentedForkReferenceTransaction ← worker-main; FORK_HOST_EXCEPTION_ACTIVATION_ID + types ← registry), fork-reference-segments/fork-gc-codec (← staying floor fork-capture-session + fork-reference-contracts), fork-early-reference-provider (imported-global recon FLOOR ← worker-main, NOT deletable), fork-activation-registry (LIVE orchestrator ← worker-main). A5 must re-home these before deleting the files.
- WALL (A5): 3 in-process instrumented-guest capture suites (fork-abort-unwind, plain-catch-payload-lifetime, fork-borrowed-replay) describe.skip'd — their NON-shared-memory guest fixtures can't coexist with the module's SHARED memory import; behavior covered by shared-memory worker e2e. Re-home onto a shared-memory harness with A5. Report: task-killswitch-removal-report.md.
- NEXT: A5 (delete the JS reference engine class + re-home the file residuals + relocate the live registry/coordinator) → A6 module-only cross-host validation (incl. browser Playwright).

## 2026-09-07 (cont) — KILL-SWITCH REMOVED: module UNCONDITIONAL (max-JS-removal met at runtime)
- c256becd5 (remove WASM_POSIX_FORK_MODULE/forkModuleEnabledBrowser flags + option/protocol plumbing; module always shipped+instantiated; :1502/:1761 fallbacks fail-loud) + 552b8ee7e (flag-off test variants→module-only) + edc4e985b (JS-engine orchestration unit tests re-homed onto a real capture module, new fork-capture-module-fixture.ts) + 79ec2c4b0 (fix dlopen-pthread fork HANG the removal exposed; A/B base 2.1s vs 30s). Pushed.
- GREP-PROOF 1: zero new ForkReferenceTransaction in non-test code (13 test-only). Class orphaned. Kept useForkModule/moduleReferenceReplay (genuine capability gates, not kill-switch); unbackable forks fail loud.
- GREP-PROOF 2 (→A5): delete-list FILES still imported for NON-engine bits: decodeSegmentedForkReferenceTransaction + FORK_REFERENCE_TRANSACTION_OWNER_ID ← worker-main; segments/gc-codec ← staying floor; imported-global recon (fork-early-reference-provider) + orchestrator (fork-activation-registry) ← worker-main. A5 = relocate live subsystems → delete dead engine.
- A5 also re-homes 6 skipped in-process instrumented-guest tests (fork-abort-unwind, plain-catch-payload-lifetime, fork-borrowed-replay) onto a shared-memory harness (non-shared-mem fixtures LinkError vs module shared memory; covered by shared-mem worker e2e meanwhile).
- Validated module-only: directed gate green (gated fails loud), continuation/abort/vfork/peer-table/dlopen-e2e/imported-globals green, host-native 45/45, typecheck+ABI clean. Browser→A6.
- NEXT: A5 relocate-live+delete-dead engine (+re-home 6 tests) → A6 module-only cross-host+dlopen+browser validation.

## 2026-09-07 (cont) — A5 DONE: dead JS reference engine physically deleted
- Commits on 79ec2c4b0: e0f4e3924 (relocate live non-engine imports off the deletable engine), ace9756b1 (physically delete fork-reference-transaction.ts + its test), 0411345c8 (re-home the in-process instrumented-guest CAPTURE tests onto shared memory). Host-TS only, no Rust, no ABI change.
- CLASSIFICATION: fork-reference-transaction.ts = net DEAD → DELETED (2541 lines; the ForkReferenceTransaction class + dead decodeForkReferenceTransactionRecord/ForkTypedReferenceReplayOwner/DecodedForkReferenceTransaction). fork-activation-registry (orchestrator), fork-early-reference-provider (imported-global floor), fork-reference-segments/-gc-codec/-recipes (wire/GC/recipe floor) = STAY. Relocations: OWNER_ID+FORK_HOST_EXCEPTION_ACTIVATION_ID already live in fork-reference-wire.ts (worker-main + registry repointed); contract/scratch types repointed to their real homes. All the deletable file's live exports were re-export shims.
- ORPHAN-EXPORT RULING: the few now-externally-unused exports in recipes/segments/gc-codec are still used INTERNALLY (type-union members / internal helpers) → live floor declarations, not dead engine; kept exported (dropping `export` = cosmetic + barrel/.d.ts risk).
- GREP-PROOF: `git grep "class ForkReferenceTransaction"` → empty; no code imports the deleted file; both files deleted; only remaining `…ForkReferenceTransaction` = staying Segmented-wire codecs (segments/wire floor) + prose comments.
- 6-TEST RE-HOME: 4 CAPTURE tests re-homed + green (fork-abort-unwind 2/2, plain-catch 2/3) via shared guest memory (`memory N 1024 shared`, wat2wasm --enable-threads) + real capture module (setCaptureModule + installTestForkCaptureModule). 2 kept precisely-skipped, carried to A6 (coverage NOT dropped, both covered by module-driven worker e2e): plain-catch fresh-CHILD reconstruction (needs module as decode BACKEND vs a fresh child instance, not just the capture floor) + fork-borrowed-replay (8-page-max shared memory models the vfork address space, child prefix at page 7 — the 5.4 MiB module can't fit without breaking the layout under test). Engine unit test fork-reference-transaction.test.ts DELETED (415 lines, dead-code coverage); early-provider + module-reconstruction fixtures rewired to the staying wire codec / minimal ForkReferenceReplayAdoptionTarget.
- LINE-COUNT DELETED: 2541 (src) + 415 (test) = 2956 dead engine lines; whole A5 diff 3075 del / 151 ins.
- VALIDATED module-only: reference-kind fresh-worker + gate 8f/10t (gated fails loud + parent survives), vfork/dlopen/peer-table 5f/17t, abort/continuation/imported-globals/from-thread 4f/48t, orchestration + re-homed units green; host-native (aarch64-apple-darwin) 45/45; tsc (tsup --dts-only) clean; check-abi green (no ABI change). Report: task-A5-report.md.
- NEXT: A6 module-only cross-host validation (Node full Vitest + browser Playwright + libc/posix/sortix as relevant) + the 2 deferred in-process reconstruction/borrowed tests.

## 2026-09-07 (cont) — A5 DONE: JS FORK REFERENCE ENGINE DELETED
- e0f4e3924 (relocate live non-engine imports) + ace9756b1 (DELETE fork-reference-transaction.ts, 2541 lines — the ForkReferenceTransaction class+algorithm; live exports were re-export shims + OWNER_ID already in fork-reference-wire) + 0411345c8 (re-home 6 in-process capture tests onto shared memory: 4 green, 2 precisely-skipped→A6, covered by worker e2e). 2956 dead lines removed. Pushed.
- STAY (floor/orchestrator, NOT engine): fork-activation-registry (orchestrator), fork-early-reference-provider (imported-global floor), fork-reference-segments/gc-codec/recipes (wire/GC/recipe floor). Grep: class ForkReferenceTransaction → empty.
- CORE GOAL MET: module unconditional, no toggle, JS reconstruction/capture algorithm gone; only host floor + module-composition layer remain.
- Validated module-only: gate fails loud, vfork/dlopen/peer-table/continuation/imported-globals green, host-native 45/45, tsc clean, ABI green.
- A6 accumulated notes: (1) add externref/GC-in-tables non-funcref scenario (A3/A4); (2) confirm the 2 deferred tests covered by worker e2e; (3) host-exception WebKit confirm (capture-session); (4) browser module-on first-ever run.
- NEXT: A6 = single module-only cross-host validation. A6a (subagent): Node Vitest full + host-native + libc/posix/sortix, real-vs-env triage, fresh artifacts. A6b (coordinator): fresh module-only browser build + Playwright fork smoke on Chromium+WebKit. Then M-SHIP curate → user merge.

## 2026-09-07 (cont) — A6a non-browser validation: A-path CLEAN, but HEAD has fixables vs main
- A-path deletion A1-A5 introduced NO new failures (every fork failure fails identically at tag 215e96923 with byte-identical artifacts). host-native 45/45, ABI green, fresh kernel+fork_module, campaign-core module-fork suites PASS. Node Vitest 3940 pass/74 fail/21 skip.
- Instrumented fork-from-thread: REGRESSION vs main? NO (empirical: main lacks the test; pre-flip tag fails identically; resume path byte-identical tag→HEAD; N1 residual #4a; fails LOUD).
- FIXABLES:
  1. REAL regression: fork_module_frames proof-of-use pollution — worker-main emits it UNCONDITIONALLY into hostDiagnostics (from P4 2bcb79c0f, NOT A5); breaks ~9 toEqual([]) tests (vfork-lifecycle-guest ×5, kernel-allocator-churn ×2, unix-listener-exec, kernel-export-failure-audit). Fix = gate the emission (diagnostic-silent contract), not relax tests.
  2. Undetermined-vs-main: fork-memory-clone-guest (exit 6), vfork-production-mechanism — fail identically at A-path start (NOT A1-A5) but not A/B'd vs main → determine regression-vs-preexisting.
  3. Coverage gap (A6-note a): peer-table round-trip is funcref-only; add externref/GC table-entry coverage.
  4. Conformance env-blocked (libc 0 cases/posix binary-resolution/sortix budget) — confirm env/pre-existing vs main (kernel/libc unchanged since H4), not a hidden regression.
- Bulk of 74 = env (missing packages, browser-in-Node ×33, GL/audio/pty, timeout artifacts). 2 deferred tests confirmed covered (vfork-fork-module; catch-ref/exnref/dlopen-e2e).
- NEXT: A6-fallout fix (gate proof-of-use + A/B the 2 undetermined + add table coverage + confirm conformance env) → re-run affected → A6b browser.

## 2026-09-07 (cont) — A6-fallout resolved (report: task-A6-fallout-report.md)
- Item 1 (proof-of-use pollution) FIXED aa28568aa + f6bde7852. Root fix, NOT a gate: a frame gate can't help (real forks commit frames — unix-listener=6, vfork=5/10/14, churn=7). hostDiagnostics is the PROBLEM channel (every other source is a failure; vfork-lifecycle asserts EXACT contents); proof-of-use success telemetry now rides a dedicated fork_module_proof msg → new onForkModuleProof host callback (Node+browser parity) → result.forkModuleDiagnostics. Zero test assertions relaxed; P4 gated-abort proof still green. No ABI change. ~9 tests + all proof suites GREEN. (kernel-export-failure-audit was MIS-attributed by A6a — it's a STATIC #handleSpawn catch audit, same unguarded catch on origin/main → H-era static-gate class, not proof-of-use.)
- Item 3 (externref/GC-in-tables coverage) CLOSED 52040e53c: added an externref table entry to the real-engine peer-table round-trip (capture via provenance→internExternref; restore via DRIVE_OP_EXTERNREF_TRANSIT→resolve_externref→STORE#2→decodeExternref; asserts wire kind 2 + canonical token on 2 workers). i31 was tried but routes through a guest drive export; externref is the clean Phase-0 path.
- Item 2 = REAL REGRESSION vs main, reported for a DEDICATED INCREMENT (not fixed — memory-layout-sensitive). A/B in ~/src/kandelo @888e628d5: vfork-production-mechanism PASSES on main, FAILS on branch; fork-memory-clone-guest env-blocked on the main checkout (no rootfs.vfs) but uses the SAME fixture vfork-production drives+passes-with on main → same regression. ROOT CAUSE (proven via trace): the forked CHILD double-reserves the co-resident fork-module region (~88 pages/5.64MB; post-fork `mmap(0x0,5641264)=0xf00000` for pid 101) on top of the inherited parent memory → child __builtin_wasm_memory_size=328 vs parent expected=240 → child _exit(3). Fix = child reuses inherited module region instead of re-reserving (worker-main fork-child + fork-module reserve + memory layout; native+Node+browser A/B). NOT A1-A5 (fails at tag 215e96923).
- Item 4 (libc/posix/sortix) = env/harness-prep, not a migration regression: libc "0 cases discovered" (nothing ran; a real libc-glue regression would run+fail cases), posix binary-resolution + sortix budget are env-invariant, libc/musl submodule pointer UNCHANGED branch↔main. Caveat: no fully-provisioned same-invocation main A/B (no installed test binaries here).
- VALIDATION: host-native (aarch64-apple-darwin) 45/45; check-abi-version.sh check GREEN (ABI 44, no drift); typecheck clean; item-1+item-3 suites GREEN. Commits: aa28568aa, 52040e53c, f6bde7852. Nothing pushed.
- BOTTOM LINE: HEAD has ZERO known regression vs main? **NO** — one remaining: the fork-child co-resident-module double-reservation (Item 2), for a dedicated increment.
- NEXT: dedicated increment for the fork-child module double-reserve; then A6b browser.

## 2026-09-07 (cont) — A6-fallout pushed; 1 regression open
- Item1 FIXED aa28568aa + f6bde7852: proof-of-use on dedicated fork_module_proof channel -> result.forkModuleDiagnostics, off hostDiagnostics problem channel; zero tests relaxed; P4 gated-abort proof intact.
- Item3 CLOSED 52040e53c: externref table-entry coverage in peer-table round-trip.
- Item4: conformance env/pre-existing.
- Pushed f6bde7852 to PR 1350.
- OPEN REGRESSION (item2): forked child double-reserves the co-resident fork-module region on top of inherited parent memory -> child memory_size 328 vs parent 240 -> child exit 3. vfork-production-mechanism passes on main, fails on branch. NOT A1-A5 [fails at tag 215e96923]. Fix in flight a91029806c2bd475f = make fork-child module reservation inherit-aware.
- NEXT after fix: re-confirm zero-regression vs main -> A6b browser -> M-SHIP.

## 2026-09-08 — fork-child module double-reserve: PARTIAL fix + residual is a design decision
- Commit 1636cffb5 (host-TS only, no Rust, no ABI): fork-child co-resident-module reservation is now inherit-aware. Report: task-child-module-reserve-fix-report.md.
- FIXED (the described bug): a COPIED fork child re-mmap'd a fresh ~88-page (5.64MB) module region ON TOP of the inherited one (child memory 240->328). Root cause proven by trace: child inherits the parent's module region + kernel mapping via the memory clone, but first-fit mmap skips the already-mapped base and grows. Fix = parent worker reports its region (base+bytes) via new fork_module_region worker msg -> host records on ProcessInfo -> COPIED child gets forkModuleInheritedBase and REUSES the base (no mmap). Borrowed(vfork) child excluded (own on-demand region, munmap'd). Grandchild propagation via childGeneration.forkModuleRegion.
- ALSO FIXED: backend resume-catalog/GC-codec staging moved off a growing channel mmap onto a dedicated 1-page staging slab reserved INSIDE the reused module region -> COPIED child no longer re-stages at its inherited (higher) mmap cursor and grows the clone. Result: child memory now MATCHES the parent (both 241) instead of exceeding it.
- Validation: fork-module instance+backend 7/7; e2e module-driven fork suites (externref/dlopen-e2e/dlopen-replay-e2e/side-module/borrowed-replay/catch-ref/exnref) 25 pass +1 pre-existing skip (NO regression); host-native 45/45; check-abi GREEN (no new ABI surface); typecheck clean.
- RESIDUAL (BLOCKED for supervised sign-off — the A6-fallout "second-order" issue): fork-memory-clone-guest + vfork-production-mechanism STILL fail (child 328->241; fixture asserts pre-fork 240). Proven cause = the co-resident module's fork-time frame chunk is allocated via a growing channel SYS_MMAP (FrameArena::new_channel, Option B), growing guest memory ~1 page at fork on BOTH parent and child equally. On main the JS fork engine keeps frames in the host-reserved fork-save buffer (0 guest pages) so the child == parent's pre-fork size. To pass, move frames/journal onto the existing fm_*_fixed_arena exports over a pre-reserved bounded arena = a memory-layout + fork CAPACITY-CONTRACT change (unbounded->bounded; lxpanel 21.5KB double-fork precedent overran the old fixed buffer => deep-fork ENOMEM regression risk; needs a Rust hybrid fixed+channel-fallback + per-worker arena overhead + cross-host/native validation). Per task guidance (STOP for ABI/memory-layout changes) reported, not made unsupervised.
- HEAD zero-regression vs main for these 2 fixtures? NO (main 240 pass; branch 241 fail) — but child now MATCHES parent and no longer double-reserves; remaining delta is the module's inherent fork-time footprint. Recommendation: (a) pre-reserved bounded frame/journal arena via *_fixed_arena + channel fallback (Rust), or (b) update the fixtures to acknowledge the co-resident fork engine's guest-memory footprint. Browser A6b not run.

## 2026-09-08 (cont) — Fix X SHIPPED: bounded in-guest fork-frame arena (fixtures GREEN)
- Y→X FALLBACK: Fix Y (MemoryManager-aware fork-time placement) was proven wasm-intractable (task-fix-y-report.md): `find_gap` is already lowest-address-first and the co-resident module cannot leave memory 0, so the committed low headroom on-demand frames reused is physically consumed by the module. Fix X = reserve a bounded fork-frame arena AT INIT (counted in `original_pages`), route all three growing fork-time allocations into slabs of it. Report: task-fix-x-report.md.
- ARENA SIZE: `FORK_MODULE_FRAME_ARENA_BYTES = 32 * 65536 = 2 MiB` per fork-capable worker (single easy-to-change line in fork-module-instance.ts). Rationale: measured single-activation cost ~3-4 pages (frame + journal + module-state); nested-fork depth does NOT multiply (COW child is replay-only, one in-flight fork at a time). 2 MiB = upper end of the report's 1-2 MiB recommendation → covers a single activation ~4x over and a ~10-16-activation dlopen fork with clear margin. Per-activation frame slab 2 pages (128 KiB, ~2x the Asyncify-era 60 KiB FORK_SAVE_BUFFER); journal slab 4 pages. Exhaustion = truthful ContinuationAllocationError(ENOMEM) → -ENOMEM fork failure; NEVER grows guest memory.
- THREE ALLOCATIONS placed: (1) frame chunk → `fm_begin_unwind_fixed_arena`/`fm_add_activation_unwind_fixed_arena` (per-activation slab); (2) journal image → `fm_serialize_journal_fixed_arena` (journal slab); (3) host-side module-state arena → a per-fork ForkModuleStateArena whose allocate() draws page slabs from the same ForkFixedFrameArena cursor (deallocate no-op; reclaimed by per-fork reset). One shared arena/worker across backend + module-state, on BOTH the process and pthread-parent paths; cursor reset once at each fork's true start (before arena.begin). Child attach releases as a no-op (never munmap a slice of its own module region).
- RUST FIX (same signatures, NO ABI surface change): `begin_unwind_fixed_arena_impl` now gates `ALLOC.reset()` on `CAPTURE_ARMED` exactly like the channel `begin_unwind_impl`. Without this the harness-only fixed export would wipe the Path B reference-capture builder that `fm_capture_begin` creates before `beginUnwind` on EVERY module fork. Rebuilt fork_module{32,64}.wasm (staged local-binaries + host/wasm + source-only-v1).
- VALIDATION: BOTH FIXTURES GREEN — fork-memory-clone-guest + vfork-production-mechanism (2 files / 3 tests) now PASS (child == original_pages + 3, boundary byte survives; borrow live_memory_delta 0 / clone +1). fork-codec host-native 432/432. check-abi-version.sh: snapshot in sync, ABI_VERSION consistent — NO bump. Broad fork sweep: fork-module-* + coordinator + capture-session-host-exception = 133 pass / 6 skip / backend-abort + backend-multi-activation updated to the fixed-arena path and GREEN. instance test asserts new region layout + exports.
- ONE non-blocking sweep item: fork-module-worker-instantiation times out at vitest's 5000ms default (test runs ~9.6s cold). Baselined A/B on the pre-change channel path = 9.91s vs Fix X 9.65s → Fix X is neutral-to-faster; the timeout is a PRE-EXISTING cold-start property of the branch, NOT a Fix X regression (passes with --test-timeout=25000).
- HEAD zero-regression vs main for the 2 fixtures? YES now — both pass; the residual from the prior ledger entry is CLOSED by the bounded arena.
- NEXT: A6b browser (fresh module-only build + Playwright fork smoke Chromium+WebKit) — NOT run here (Node-host + host-native + ABI validated).
- A6b BROWSER (2026-09-08, commit e69ea9f83): BLOCKED on fork execution; two fixes landed. (1) Projection/build seam RESOLVED — browser fetches projection-owned fork_module32.wasm (200, hash 93ba376f) on BOTH Chromium + WebKit, NO ownership error. (2) Build regression FIXED: unconditional fork module broke every build-time kernel boot (coreutils-docs man capture, opcache prewarm, wordpress/mariadb preinstall) with "source-only-v1 requires WASM_POSIX_SOURCE_ONLY_BINARY_ROOT"; injected fork module explicitly (NodeKernelHost.forkModuleBytesByWidth → init msg → kernel-worker forkModuleInitFields; opcache-prewarm + wordpress-preinstall load staged host/wasm via findRepoRoot). All 7 browser products build (local-build --json outcome:succeeded); verify-fresh exit 0. (3) BLOCKER — browser fork ENGINE traps at runtime: Chromium nested fork (pipeline/bg-job inside `$()`) → "unreachable" at __wpk_fork_unwind_transport_direct_0 (fork-worker-import-exceptions.ts:437/513); single-level forks OK. WebKit → "WebAssembly.Module.exports unable to produce export descriptors" at centralizedWorkerMain (worker-main.ts:2292), fork-child module, blocks fork entirely. Node forks (heavy PHP-FPM/MariaDB during build) work w/ same module → Node-browser parity break exposed by removing the JS fallback. Report: task-a6b-report.md; screenshots task-a6b-{chromium,webkit}.png. Server left running http://127.0.0.1:5461/?demo=shell (pid 41121). Pre-existing unrelated: prepare-browser exits 1 on a SUCCESSFUL build (dev-shell banner corrupts cmd_local_build result JSON, jq col-8 parse error).
- BROWSER FORK FIX (2026-09-08, HEAD e69ea9f83 -> 2dd5f5274): PARTIAL. (1) WebKit "unable to produce export descriptors" FIXED (commit 2dd5f5274) — worker-main.ts probed the fork-child module via WebAssembly.Module.exports() DIRECTLY; WebKit throws on ABI-44 fork modules. Routed through wasmModuleExports() (exact-bytes reflection registered ~ln3310), matching every other reflection site. WebKit now boots + single-level forks work = exact Chromium parity. (2) prepare-browser exit-1-on-success FIXED (commit 28d038cbe) — NOT the dev-shell banner (A6b guess); crates/fork-module/build-wasm.sh wrote progress + "matches current source" to STDOUT, bleeding into the local-build result JSON that jq validates. Routed all informational lines to stderr (kept $()-captured build_key_path/cat). local-build --json now clean, prepare-browser exits 0. (3) Chromium "nested fork" trap = BLOCKED-DEEP, NOT a browser parity break. Temp-diag in replaceThrown unmasked the fatalTrap: the real origin is fm_capture_begin (crates/fork-module/src/lib.rs:4064, ReferenceGraphBuilder::begin) trapping unreachable; __wpk_fork_unwind_transport_direct_0 is just the downstream re-trap. Reproduces IDENTICALLY on Node (clean NodeKernelHost bash harness, source-only env: every $()/pipeline fork empty, same ForkReferenceCaptureModule.begin stack). Needs fork-module/fork-codec (Rust) reference-capture work — STOPPED per task. Both engines now fail ONLY on this shared defect (BAREPIPE/EXPR ok, PIPEOUT/BGWAIT empty). cargo test -p fork-codec 432 passed; check-abi-version NO bump. Report: task-browser-fork-fix-report.md; screenshots task-forkfix-{chromium,webkit}-final.png. Vite left running http://127.0.0.1:5461/?demo=shell.

## 2026-09-08 (cont) — CAPTURE-BEGIN TRAP ROOT-CAUSED + FIXED; deeper decode defect BLOCKED-DESIGN-GAP
- Commits: 252cd40b0 (fork-module COW-inheritance reset), cb11b9c6b (masked-trap diagnostics), a9212419b (nested cmd-subst regression test). Report: task-capture-begin-fix-report.md.
- ROOT CAUSE (the named bug): trap is `*capture_state() = Some(builder)` in fm_capture_begin — dropping the PREVIOUS fork's resident ReferenceGraphBuilder (pinned via a temp CAPTURE_DEBUG_STEP export: reached step 6 = builder built, trapped on the store/drop). The builder + bump heap + per-worker catalogs live in the fork module's BSS, which sits INSIDE the guest shared memory at __memory_base (PIC placement); a COW child's memory is a CLONE, wasm doesn't re-zero BSS, so the child inherits the parent's populated module state. fm_capture_begin reset the bump and reallocated a fresh builder at the same low addresses BEFORE dropping the old one; ReferenceGraphBuilder's BTreeMap Drop then walked clobbered nodes → `unreachable`. NOT a Fix-X regression (Fix-X's CAPTURE_ARMED gate is correct + preserved); NOT a capture-path design gap — a lifecycle/ordering BUG.
- FIX 1 (252cd40b0): reset_bump_heap() drops all bump-allocated statics with non-trivial Drop (state/capture_state/serialized/drive-plan) BEFORE the reset; used at fm_capture_begin, the non-armed begin_unwind / begin_unwind_fixed_arena branches, and begin_child_replay (COW child). Armed begin_unwind still skips reset (keeps live builder).
- FIX 2 (same commit): unmasked SECOND COW-inheritance bug — the per-worker "seeded once" catalogs (funcref catalog, funcref/static-root bases, GC codec, host-exception owner, resume catalog) are inherited by a COW child and reject re-seed with EINVAL (fm_set_activation_gc_codec errno 22). Reset them in set_format_impl (the once-per-worker call before all seeding).
- DIAGNOSTICS (cb11b9c6b): replaceThrown surfaces the inner RuntimeError message+stack (via onFatal always; error channel gated by WASM_POSIX_FORK_TRAP_DIAG=1 / globalThis.__wpkForkTrapDiag, because the same branch carries the EXPECTED kernel_exit child-exit trap). Stops the fatalTrap misattribution to __wpk_fork_unwind_transport.
- REGRESSION (a9212419b): Node bash-fork vitest — nested cmd subst `echo $(echo $(echo hi))` (COW child forks again). FAILS (empty output) on pre-fix module, PASSES after; TDD fail→pass proven by rebuilding module from 2dd5f5274 source and back. Loop of cmd substs also asserted.
- VALIDATION: Node — echo $(echo hi), nested cmd subst, sequential cmd substs, loop, true & wait all fork OK. fork-codec 432/432. 55 fork-module fixtures pass (no regression). check-abi: snapshot in sync, ABI_VERSION consistent — NO new bump (fork-module internal logic + host diag + test; no ABI surface).
- BLOCKED-DESIGN-GAP (defect 3, coordinator decision): fixing 1+2 unmasks a reference-graph DECODE out-of-bounds — fm_decode_reference_graph (decodeReferenceGraph, fork-module-backend.ts:508) traps "memory access out of bounds" on a pipeline/nested fork's inherited KFMS arena. Probe: root pointer valid+in-bounds (0xe30000<18350080), valid KFMC/KFMR header; OOB is DEEP in the multi-record reference-transaction decode for the arena shape real bash pipeline forks produce (no fixture covers it). Distinct decode-path defect; per the task's stop clause, reported not force-fixed. So `echo $(echo hi | cat)` now reaches defect 3 (was the capture trap).
- BUILD-FRESHNESS: build-wasm.sh does NOT write local-binaries/source-only-v1/ (only local-binaries/ + host/wasm/); resolveBinary("fork_module32.wasm") resolves the source-only-v1 copy, so every rebuild was followed by cp host/wasm/*.wasm → source-only-v1/. Missing this silently runs the stale module.
- BROWSER: `./run.sh browser --port 5461 --strictPort` — result appended below when the build/playwright completes.
- BROWSER RESULT (2026-09-08, playwright-core chromium-1234 + webkit-2336 vs ./run.sh browser --port 5461 --strictPort, ?demo=shell): EXACT 3-host parity. Both engines booted=true, pageErrors=[]. single cmd subst (SINGLE=hi), nested cmd subst `echo $(echo $(echo hi))` (NEST=hi), and bg job `true & wait` (BGDONE) all PASS — the capture-begin fix holds cross-host (nested cmd subst trapped pre-fix). pipeline-in-subst `echo $(echo hi | cat)` FAILS with the SAME host-agnostic decode OOB (defect 3): fm_decode_reference_graph "memory access out of bounds" wasm-function[162]<-[338]<-[207]<-[417]<-[317]<-[486]<-[125], decodeReferenceGraph@fork-module-backend, bash prints "fork: Out of memory". Identical on Chromium + WebKit + Node. Screenshots task-capturefix-{chromium,webkit}.png. Server stopped after validation.

## 2026-09-08 (cont) — DEFECT #3 FIXED + family flushed (commit 96364edee)
- ROOT CAUSE (encoder vs decoder = NEITHER): the pipeline arena is VALID and the fork-codec decode is CORRECT — proven by dumping the child's full guest memory (18350080 B, root 0xe30000, pw4) and replaying decode_module_state + decode_segmented_reference_transaction NATIVELY (debug AND release): decode SUCCEEDS, nodes=1. The wasm trap is a resident-STATE Drop bug. `fm_decode_reference_graph`'s `*decoded_graph() = None` DROPS the COW-inherited `DECODED_GRAPH` (a subshell that decoded, then reused its bump, then forked the pipeline); the inherited `SegmentedReferenceTransaction`'s BTreeMap child-pointers point into the parent's since-reused bump = clobbered → Drop walk dereferences a garbage pointer OUT of guest memory → "memory access out of bounds". Pinpointed with temp stage-marker exports read AFTER catching the trap (a wasm trap unwinds the call but leaves the instance usable): whole decode completed (step 4), fault was the pre-decode `= None`.
- UNMASKED same-family site: `begin_reference_replay_impl` reclaimed `reference_state`/`reconstruction_state`/`reference_feed` with `*slot = None` too — inherited REFERENCE_STATE owns a transaction → same clobbered walk → `unreachable` "before replay readiness".
- FIX: new `abandon_resident<T>(&mut Option<T>)` = `core::mem::forget(slot.take())`. None of these resident types has a side-effecting Drop (verified: no impl Drop) — they own only bump memory (reclaimed by ALLOC.reset; dealloc no-op) + scalar leaves — so forget is semantically identical, frees no real resource, and skips the unsafe walk. Applied at EVERY reclaim site to flush the whole family: decode_reference_graph_impl (decoded_graph), begin_reference_replay_impl (reference_state/reconstruction_state/reference_feed), reset_bump_heap (state/capture_state/CAPTURE_SERIALIZED/DRIVE_PLAN, converted from `= None`). NO encoder/decoder change. NO ABI change (fork-module-internal reclaim; snapshot in sync, no bump).
- Earlier wrong turn (recorded so it isn't repeated): resetting the bump + forgetting statics in set_format_impl fixed decode but REGRESSED nested-cmdsubst (broke reconstruction). The bump was never the problem; the minimal correct fix is forget-at-the-drop-site, not a set_format bump reset.
- SHAPE MATRIX (Node NodeKernelHost, module ON): PASS single/nested cmd-subst, subshell, bg+wait, 2>&1, for-loop-subst, while-loop, &&/||, ; list. Pipeline-in-subst + all `| cat` shapes now REACH cat exec and fail ONLY on the harness `cat` ENOEXEC (coreutils multicall not exec-registerable in the bare harness — same artifact the prior report noted; NOT a fork defect). Proven with builtin-only pipe segments: `echo a | while read x; do echo got$x; done`=gota, `echo $(echo b | while read x; do echo $x; done)`=b, multi-stage pipe=rl1/rl2 all PASS — pipeline-in-command-substitution forks + decodes + replays end-to-end.
- REGRESSION TEST: host/test/fork-pipeline-command-substitution.test.ts (new). TDD proven fail→pass: pre-fix module (built from a9212419b via git stash) FAILS "memory access out of bounds"; fixed module prints b. + nested regression still passes (no regression).
- VALIDATION: fork-codec 432/432 (aarch64). fork-module host suite 17/17 (reconstruction/backend-abort/multi-activation/instance/memory-clone-guest/vfork) no regression. Pipeline+nested host regressions 4/4. check-abi-version: snapshot+headers+TS in sync, NO bump. Build-freshness: build-wasm.sh + cp host/wasm/*.wasm → local-binaries/source-only-v1/ (hashes fork_module32 873b0312.., fork_module64 37f16957..; build-key f580c192).
- BROWSER (playwright-core chromium + webkit vs ./run.sh browser --port 5461 --strictPort, ?demo=shell): 3-host parity, ALL GREEN. Both engines booted=true; pipeline-in-subst (builtin) START_b_END, nested N_hi_END, bg BG_DONE all PASS; 0 console / 0 page / 0 failed-request errors. LITERAL defect #3 with REAL cat (Chromium): `echo $(echo a | cat)`=LIT_a_END, `$(printf x | cat | cat)`=LIT3_x_END PASS, 0 errors. Screenshots task-pipeline-{chromium,webkit}.png. Server stopped.
- BROWSER FRESHNESS SEAM (orthogonal, [[fork-module-not-projected]]): a fork-module rebuild that changes the wasm SIZE makes `./run.sh browser` boot 500 on browser-fork-module-artifact.ts ("member size is 133818, expected 133754") because it trusts the cached source-only projection manifest. Fix for validation: rm local-binaries/source-only-v1/.kandelo/source-only-program-projection-v1.json && ./run.sh prepare-browser (regenerates from current local-binaries → records 133818/873b0312). Not a runtime defect; a projection-freshness gap in the out-of-band fork-module wiring. Report: task-pipeline-decode-fix-report.md.
- GC-FORK FIX (commit d32d8a206): 4 host-native GC/static-root reconstruction tests (smoke_fork_gc_{array,struct,two_object_cycle}, smoke_fork_static_root) failed `fm_build_gc_plan failed: errno 22`→pump-timeout. REGRESSION, bisected to 252cd40b0 (NOT Fix X, NOT 96364edee): its blanket `fm_set_format` catalog reset zeroed ACT_GC_CODEC_ACT_COUNT on the COW child, destroying the inherited GC codec catalog that the native host relies on (native does NOT re-seed the codec on a child — guest.rs:4516; only Node/browser worker-main.ts:5099 re-seeds). Empty codec map → GcCodecHints::new EINVAL. FIX: make set_activation_gc_codec_impl idempotent on an identical re-seed (conflicting bytes still EINVAL) + drop the GC-codec portion of the fm_set_format reset (kept capture-side resets). Serves both hosts (inherit vs re-seed) with no reset; no ABI/wire/plan change. VALIDATION (aarch64): 4/4 target PASS; full host-native lib 45 passed/0 failed/4 ignored; fork-codec 432/432; check-abi-version in sync, NO bump. Report: task-gc-fork-fix-report.md.
- P-10/P-11 CONTINUATION-ARENA FIXES (commit 823c20086): fork-instrument-coverage P-10 + P-11 were red. BASE COMPARISON (faithful, built base kernel+fork-module at 1636cffb5): P-10 PASS@base / trap@HEAD = Fix X regression; P-11 FAIL@both (never passed — mid-capture-abort traps at base too; Fix X additionally retired its address-space-starvation mechanism). Fixtures/test byte-identical since ABI-42 (b68524efa8), untouched by Fix X. ROOT CAUSE (traced): Fix X sub-divided the 2 MiB arena into a fixed 128 KiB per-activation frame slab; a single deep activation (P-10's 4096 frames) overflows it → module ChunkAllocator ENOMEM mid-unwind; the Phase-6 D5 import flip binds __wpk_fork_frame_reserve straight to the module export, bypassing the JS onReservationAbort→beginCaptureAbort, so the coordinator stays "capture" and the guest re-entry traps ("fork import reached while capture"). No module-mode partial-capture-abort exists. P-10 FIX (host-TS only, no ABI): give the SOLE activation of a single-activation fork the whole arena minus journal+module-state reserves (singleActivationFrameBudget) so a deep linked continuation grows up to the bound; multi-activation (dlopen) unchanged at 128 KiB/activation; arena stays 2 MiB. P-11 BLOCKED-DESIGN: (1) the truthful mid-capture-exhaustion abort is a pre-existing gap — correct integration is worker-main.ts:5354 sealCapture()+beginAbortReplay(errno), but the failure surfaces as a kernel_fork capture re-entry and the guest only abort-rewinds after wpk_fork_abort_begin; two attempts gave `unreachable`(drive mid-select_unwind_frame) / silent-hang(finalize w/o rewind) → reverted, not shipped; (2) with the P-10 fix, single-activation arena exhaustion is UNREACHABLE in production — the fixture's C shadow stack is ~100 KiB (__stack_pointer init ~110 KiB, memory initial 2 pages → ~4-5k frames) < the ~10k-frame arena budget, so the C stack overflows before the arena; P-11's premise no longer maps to a reachable state. Per task, P-11 NOT relaxed to green (would paper over). VALIDATION (aarch64): P-10 green; fork-module backend 7/7; fork-codec 432/432; host-native 45/0/4; check-abi-version in sync, NO bump. Base scratch worktree removed. Report: task-p10-p11-fix-report.md.
- GUEST-ALLOCATOR CONTINUATION-FRAMES PROBE (no commit of code; report only): hypothesis = allocate LinkedForkContinuation chunks through the GUEST's own malloc/free (one allocator ⇒ coherent memory-0 view ⇒ dynamic/unbounded, dissolving Fix X's cap + P-11 mid-capture-exhaustion trap). VERDICT: PROBE-BLOCKED — BLOCKED-NEEDS-GUEST-ABI. Three independent verified blockers: (1) REACH/guest-ABI gate — guest wasm exports NO malloc/free/__mmap (verified fork-memory-clone.wasm Export[53]; __mmap=func[97]/elem[104] unexported); export set is fixed by SDK link flags (sdk/src/lib/flags.ts:265+, wasm32posix-cc:370+) + fork-instrument (adds only wpk_fork_* / codec stubs) — neither force-exports the allocator; the fork-module imports no allocator and is instantiated BEFORE the guest (fork-module-instance.ts:310-324,681); reaching guest malloc ⇒ -Wl,--export=malloc,free + relink+re-instrument every fork-capable package = fork-capable-guest artifact-contract change = the Phase-1.1 STOP gate. (2) MECHANISM REFUTED — musl is mallocng, which sources ALL memory from mmap(0,...) (malloc.c:249,310) → __mmap → SYS_mmap → kernel find_gap(hint=0), the SAME allocator/placement the module channel path uses and that task-fix-y-report proved COLLIDES with the guest's raw __builtin_wasm_memory_grow pages; guest malloc is NOT a distinct coherent view, so it reproduces the clobber rather than dissolving it, and delivers no unbounded no-clobber growth. (3) REENTRANCY — musl malloc is lock-holding/non-reentrant; guest is frozen at an arbitrary point (possibly inside malloc) mid-capture-unwind, so a module→guest-malloc call can deadlock/corrupt the heap. STOP per task; Fix X (bounded 2 MiB arena, 823c20086) KEPT as fallback, untouched. No code changed → cargo/host-native/check-abi unchanged by construction; browser N/A. Report: task-guest-allocator-frames-report.md.
- PHASE 0 GROWABLE-ARENA PROBE (commits a103464dc scaffolding + 8818be7fd test; Fix X + production UNCHANGED, all behind env KANDELO_FORK_PROBE_GROWABLE_ARENA=1): VERDICT PROBE-GREEN. Settled empirically on Node that Fix X's 2 MiB cap is a FIXTURE ARTIFACT of fork-memory-clone.c's raw __builtin_wasm_memory_grow, not a real placement/headroom constraint. Growable "Option B" channel allocator is fully intact in the module (fm_begin_unwind/fm_add_activation_unwind/fm_serialize_journal_alloc + FrameArena::new_channel, still in FORK_MODULE_REQUIRED_EXPORTS); enabled via a backend toggle (probeGrowableChannelArena) + worker-main env plumbing (routes frame chunks + journal + module-state through the channel) — a contained toggle+`cd host && npm run build`, NO module rebuild, NO surgery. NEW real-growth fixture host/test/fixtures/malloc-deep-fork.c grows heap via malloc(6 MiB)→mallocng→mmap(0,…)→SYS_mmap (kernel-TRACKED find_gap), then forks from a target-deep recursion. EVIDENCE (KERNEL_SYSCALL_LOG page numbers): module@149(~120pp), guest malloc@271..366 (TRACKED), meta@368-369, fork-time frame chunks land at page 370+ — ABOVE the tracked malloc region, find_gap SKIPS it, ZERO clobber (child re-verified all 96 malloc pages). depth=16000 (16007 frames): GROWABLE exit0 pages=397 child replays all 16007; SAME fork under default Fix X FAILS (arena exhausts between depth 8000=pass/16000=fail → capture crash "fork import reached while capture", worker exit -1) = cap genuinely bites deep forks. Truthful OOM: maxProcessMemoryBytes=40MiB → ProcessMemoryCapacityError at child clone → fork()=-1 to guest, parent survives, no clobber. CONTROL: the UNCHANGED raw-grow fork-memory-clone under growable → exit6 "child lost boundary byte" (clobber reproduces ONLY for untracked grow). CORRECTS the guest-allocator probe's Blocker-2: musl mallocng DOES go through find_gap, but because the malloc region is a TRACKED mapping find_gap skips it → NO collision; the collision is specific to untracked __builtin_wasm_memory_grow, not to find_gap-placed growth. Regression (probe OFF): fork-memory-clone-guest + vfork-production-mechanism + 2 backend tests 5/5 pass, production Fix X unchanged. Node only (no browser/conformance/ABI — channel exports already ship, no ABI-relevant change). RECOMMEND Phase 1: revert Fix X, restore growable channel allocator with truthful ENOMEM/MAP_FAILED. Report: task-phase0-growable-probe-report.md.
- PHASE 0.5 BUILD-FRESHNESS (commit f1bcc4091, DONE): closed the ship-relevant stale-artifact seams before the fork-module/kernel rebuild storm. Defect (1) kernel cache-key omits kernel/runtime-core inputs — ALREADY closed by d44bc1259 (Stage 1): build.toml uses `cargo:kandelo`, cargo_closure.rs::cargo_closure_paths BFS-walks the workspace-member dep graph (crates/kernel+runtime-core[incl netif.rs]+shared+.cargo/config.toml), and the guard test registry_packages_that_cargo_build_a_workspace_crate_declare_its_closure_input generalizes it. VERIFIED empirically: kernel compute-cache-key-sha changes d3a0bd66→947ce4bb on a netif.rs touch (reverts); removing "cargo:kandelo" fails the guard test with the exact message. Defect (2) fork-module projection seam — ALREADY closed by d897f7027 (closure build-key stamp in build-wasm.sh via workspace-closure-sha) + 020770377 (local_build.rs projects fork_module{32,64}.wasm as SourceOnly-owned members: ensure_coresident_fork_module_built rebuilds if stale, coresident_fork_module_projection verifies stamp==closure, re-stages). NEW WORK = the deferred L5 verify-fresh gate: verify_fresh_report only checked kernel.wasm, so standalone `xtask verify-fresh` was green while a stale projected fork-module 500s the browser (pinned-projection member-size check). Added verify_fresh_coresident_fork_module + pure check_projected_fork_module_freshness (reads source-only projection manifest; fails on node.cacheKeySha256!=current closure [stale-vs-source] OR staged size/sha!=manifest member [stale-mirror = the direct 500]); 5 new unit tests. VERIFIED: live worktree projection was stale (manifest fork_module32 size 133818/873b03 vs staged 133903/2f819c; node key f580c19 vs closure b548f329) → verify-fresh now exits 1 with actionable msg (was exit 0); fork-module closure-sha b548f329→a52b6b0f on a src touch (reverts). Full xtask test suite exit 0; check-abi-version in sync, NO ABI bump. RESIDUAL: live projection still stale (gate flags it; clearing needs a local-build re-projection — NOT run, heavy); auto-resync end-to-end not demonstrated (pre-existing 020770377 path). Report: task-phase0.5-build-freshness-report.md.
- PHASE 2 MODULE-MODE PARTIAL-CAPTURE ABORT (commits 0ec38eb6c host + c432b22d2 fixture; base 608194508 = retire Fix X, growable channel mmap is the only capture path): DONE — P-11 GREEN. The DEEP P-11 case trapped: the Phase-6 D5 import flip binds the guest __wpk_fork_frame_reserve straight to the module export, so a mid-unwind channel_mmap failure set fm_last_errno=12 and returned 0 with NO host notification → coordinator stayed "capture" → the guest's abort restart loop re-entered kernel_fork in capture phase → "fork import reached while process continuation is capture" worker-fatal trap (parent killed). GUEST-PROTOCOL TRACE (crates/fork-instrument/src/instrument.rs): __wpk_fork_select_unwind_frame (:663) returns 0 on reserve==0 after writing the abort scratch, its own comment (:685) = "frame_reserve == 0 synchronously moves the host runtime to abort replay"; the postamble (emit_static_call_unwind_handler :3559) then br's abort.restart_loop, which ASSUMES it already runs in STATE_ABORT_UNWINDING (set by wpk_fork_abort_begin). The JS capture path honored this via continuationImports' reserve closure → onReservationAbort → beginCaptureAbort (nested, synchronous). FIX: wrap the flipped module reserve (process AND pthread-parent paths, worker-main.ts) to mirror onReservationAbort — on 0, read fm_last_errno FIRST then call new coordinator beginModuleCaptureAbort(errno) (fork-process-continuation.ts): backend.sealForAbort() = fm_finish_unwind ONLY (seals writers+journal; a failed reserve leaves pending==None so the committed chain seals; NO journal serialization=no child, NO guest wpk_fork_unwind_end=guest is mid-unwind — the exact thing the two PRIOR attempts got wrong → their unreachable/hang) → registry.sealCapture()+publishProcessLaunchRoot → phase sealed-parent → reuse the PROVEN beginAbortReplay (→beginModuleAbortReplay→fm_begin_abort attaches drivers over the sealed in-memory journal + wpk_fork_abort_begin→ABORT_UNWINDING). Guest replays committed frames, re-enters kernel_fork in abort-replay → finishAbortReplay→finishModuleTransaction(true)→fm_finish_abort munmaps chunks → fork()=-1/ENOMEM, parent intact. ROOT case unchanged (fails at arena.begin() before capture → ContinuationAllocationError → -errno; guest never unwinds). FIXTURE CALIBRATION (c432b22d2): after the trap fix P-11 reached CONTINUATION_ENOMEM:ok then failed on the RECOVERY fork at fm_serialize_journal_alloc ENOMEM — a SUCCESSFUL module fork needs THREE pages (metadata arena + frame chunk + journal image) but the fixture froze a 2-page budget from an earlier model; freed+probed 3 (real module cost); the 4096-deep fork still fails mid-unwind after committing (needs many more chunks). Backend method sealForAbort()+lastErrno() added; new fm_last_errno/fm_finish_unwind already exported (no ABI). VALIDATION (Node, aarch64): P-11 PASS; P-10 PASS; P-01..P-09 9/9; fork-module-backend-abort 1/1; fork-codec 432/432; host-native 45/0/4; check-abi-version in sync, NO ABI bump (host-TS + fixture-C only). Host parity: wrapper in shared host/src on both fork paths (Node validated; browser re-validation recommended, not host-specific). LATENT (out of scope, noted): a fork that completes unwind then fails at SEAL-time fm_serialize_journal_alloc still throws through sealCapture at the completion handler (worker-main.ts:~5273) instead of routing to beginAbortReplay — P-11 no longer hits it. Report: task-phase2-module-abort-report.md.
- PHASE 3 MODULE-BACKS-EVERY-FORK (commits 35a4e95a8 host+module-cap + 974e11c1b proof test; DONE): closed the three `useForkModule=false` fallbacks at worker-main.ts:~3747 so the co-resident module backs EVERY fork; genuine impossibilities FAIL LOUD (no silent JS twin). CASE 1 (ptr width) = ALREADY SATISFIED: correct-width module instantiated on demand per guest via forkModuleInitFields(detectPtrWidth(bytes)) (node lazy Map by width; browser ships only wasm32, wasm64 browser guest fails loud); a guest-internal width mismatch already threw at :3599 → the `ptrWidth===linkedFrameFormat.ptrWidth` predicate term was DEAD, removed. CASE 2 (resume catalog > 16384) = RAISED to 65536: the cap is a fork-module BSS array (crates/fork-module/src/lib.rs RESUME_CATALOG_CAP + ACTIVATION_CATALOG_ORD_CAP), NOT a pure host cap. REAL DATA (KFRC section census): php-fpm 19190, php 19026, node/spidermonkey/js 16555 ALL EXCEEDED 16384 and were SILENTLY on the JS twin (a Phase-4 fatal landmine); ruby 8579, wget 6833, git-remote-http 6266, cpython 6192, rest <4500. Raised the module BSS caps + host/src/fork-module-backend.ts + crates/host-native/src/guest.rs to 65536, and grew host/src/fork-module-instance.ts FORK_MODULE_STAGING_BYTES 1<<16→1<<18 (256 KiB) so a COPIED child stages its catalog into the inherited slab in place (a growing mmap would break the fork memory-clone size invariant). Removed the silent fallback: process path no longer gates on the cap (backend ctor throws loud on overflow); pthread coordinator path now throws on overflow instead of null-instance→{}-imports→JS. Module mem_info memorySize grew ~384 KiB (4592680→4985896; wasm FILE size unchanged 133903 since BSS is zero-init); region reserved dynamically from mem_info so parent/child stay consistent. NOT an ABI/guest-artifact change (cap is module-internal BSS; guest catalog is READ not re-instrumented; fork-module is host-side; mem_info read dynamically) — check-abi-version + xtask verify-fresh both in sync, NO bump. CASE 3 (fork-from-thread child lacking wpk_fork_resume_thread) = HOST-ONLY, NOT flag-and-stop: emit_fixed_resume_boundaries (fork-instrument/src/instrument.rs:404,4516-4579) emits the export unconditionally for any guest exporting __indirect_function_table, which every pthread/fork-from-thread-capable guest necessarily has (landed with ABI 43); a child missing it = stale artifact → now FAILS LOUD (rebuild) instead of silent JS. No guest-instrument change required. REBUILT+SYNCED fork_module{32,64}.wasm (local-binaries/host-wasm/source-only-v1, all gitignored) + re-finalized SourceOnly projection via ./run.sh local-build (98/98; manifest fork_module32 133903/9c66454f). VALIDATION (Node + aarch64, dev-shell): NEW large-catalog proof (fork-module-instance.test.ts) 20000+65536 errno=0 / 65537 E2BIG 6/6; worker-instantiation real fork 1/1; fork-from-thread 3/3; fork-continuation+process-continuation+resume-catalog 44/6-skip; vfork-fork-module+malloc-deep-fork+backend-multi-activation 4/4; host-process-pointer-width 20/20; fork-module harnesses ALL PASS; host-native lib 45/0/4; fork-codec 432/432; check-abi-version in sync NO bump. NOT RUN: browser Chromium/WebKit (shared host+module change, projection re-finalized so should boot — re-validation recommended pre-Phase-4); conformance suites (deferred to batched phase). RESIDUAL: cap still fixed 65536 (fails loud past it, not unbounded); fully-dynamic module-owned catalog is a possible follow-up. JS twin NOT deleted (that is Phase 4). Report: task-phase3-module-backs-every-fork-report.md.

## 2026-09-08 — PHASE 4 COMPLETE (point of no return)
- JS continuation twin DELETED (eeea97c94, +117/-4643 across 11 files); module-or-fatal enforced (55fa39452); seal-time journal-image OOM routed to truthful abort-replay (94c8d117a). Rust module is the ONLY capture/replay path.
- Validated post-session-restart by controller: fork Node suite 13/13 files; host-native lib 45/0/4; fork-codec 432/0; check-abi-version consistent (ABI 44 in sync); xtask verify-fresh incl. L5 fork-module gate green; host npm run build typecheck clean. Module-path suite covers all deleted twin-test scenarios [abort=P-11/backend-abort; borrowed=vfork-production/dlopen-e2e; catch/exception=fork-instrument-coverage C-*/S-08]. Browser owed at Phase 6 batch.
- NEXT: Phase 5 (collapse movable TS orchestration into Rust; drop proof-of-use counters; fm_* 84 -> ~40-45; add Rust-first-for-kernel-and-fork agent guidance).

## 2026-09-08 — PHASE 5-B (reduce fork TS glue) — NEEDS-DEFER-DECISION, no code landed
- Investigated all 3 named reductions on HEAD d9ef8e3bf (already past Phase 4; ForkFixedFrameArena already retired, production on channel-mmap Option B). fm_* surface = 84 (unchanged). Baseline: host npm run build clean; fork-module-gc-replay + fork-module-backend-abort 6/6 PASS (they read the counters as their sole assertion oracle).
- Step 1 (drop 11 proof-of-use counters): NOT "nothing depends on them" — the counters are the SOLE exact-count correctness oracle of the module's isolated unit tests (gc/exnref/funcref/decode-scan/multi-activation/backend-abort assert toBe(2)/toBe(HANDLES.length)/toBe(COMMITS.length)), AND fork_module_frames/fork_module_proof are a cross-host (Node+browser) diagnostic bridge → sound completion is a Phase-6 cross-host increment; drop-vs-fold-into-fm_stats is a coverage judgment. Recommend FOLD into fm_stats at Phase-6 batch.
- Step 2 (allocator bookkeeping into Rust): already DONE; only test-only _fixed_arena residue (trampoline + borrowed-replay harnesses) remains — deleting = channel-mock rewrite, zero production benefit. Recommend leave.
- Step 3 (coarse fm_* entries, 84->~40-45): plan's highest-risk Phase 3 — browser-validation-gated (Chromium nested-fork + WebKit historically fragile; task is Node-only, browser batched Phase 6) + guest-unwind state-machine hazard (mid-select_unwind_frame abort trapped in 2 prior attempts) + possible guest-ABI gate (wpk_fork_rewind_begin/end must be drive-table-eligible). Defer to Phase-6-batched increment.
- No destructive edits: landing any under Node-only evidence would violate validation/host-runtime contracts or degrade coverage unilaterally. Report: task-phase5b-reduce-glue-report.md. Awaiting maintainer ruling.
- Option A ruling executed. R1 DONE (commits c1aa5adfe fold + fd8cb33f5 codegen fix): folded the 11 proof-of-use counter exports into ONE fm_stats(field)->i64 field-indexed accessor. fm_* exports 84->74. Coverage NOT degraded — the exact-count oracle preserved (tests read fm_stats(FmStatField.X); backend proof methods keep names + delegate; diagnostic bridge/protocol unchanged). Field-index contract lockstepped across lib.rs / host-native guest.rs (FM_STAT_* + fold_fork_proof_of_use) / fork-module-backend.ts (FmStatField enum) / harness.mjs (FM_STAT). NON-OBVIOUS BUG: a match over 11 atomic loads => br_table that MISCOMPILED post-injection (arms read stale 0 while an in-scope diagnostic reader saw the real value; an unrelated early-return flipped the arm) — fixed by a [&AtomicU64;11] bounds-checked .get().load() (single computed-address load, no br_table). Node validation: fork-codec 432/432, host-native 45/45, harness PASS, all counter tests (gc/exnref/funcref/multi-activation/externref/decode-scan/gc-cycle) PASS, backend-abort/multi-activation/malloc-deep-fork PASS, fork-continuation PASS, vfork-production-mechanism 2/2, fork-from-dlopen-side-module-e2e 7/7. check-abi-version.sh: consistent, NO bump (host<->module internal, guest-ABI gate NOT tripped). Pre-existing (verified at parent d9ef8e3bf): fork-instrument-coverage runFixture times out (D-01 times out at parent too; zero fm_*/fork_module refs) + xtask verify-fresh fails to COMPILE errno/getrandom/lzma-sys/bzip2 (env). 
- R2 (coarse fm_* collapse) NEEDS-DEFER-DECISION. Acted on the maintainer "mechanical/low-risk, batch validation" cadence change by investigating the actual host call graph: the fine-grained exports are NOT redundant wrappers of the coarse entries — they are LIVE in DISTINCT production paths (fm_begin_reference_replay<-fork-reference-recipes.ts; fm_restore_from_arena<-fork-table-snapshot.ts; fm_attach_child<-4 files; fm_build_gc_plan = test-oracle only). A real export cut = UNIFYING those divergent paths (which lands in the reentrant module->guest->module drive + useForkModule selection at worker-main:~3752 = browser-gated) + rewiring ~14 direct-export unit tests. NEW guest-ABI evidence: __wpk_fork_drive_table is host-populated by retrieving guest exports BY NAME, so binding wpk_fork_rewind_begin/end needs NO fork-instrument change — only a drive-table stride widening (fm_drive_table_base = activation*3), ABI-neutral, re-verify with check-abi-version.sh. STOP reasons per group: A (parent_replay rewind drive steps) + D (child_attach residue) = browser-gated reentrant-drive extension (Node!=browser per validation contract); B (fm_abort_replay + P-11) = guest-unwind state-machine hazard (2 prior mid-select_unwind_frame attempts trapped); C (parent_capture) straddles the JS-only run-loop exception catch. Report: task-reductions-report.md. Flagged the mechanical-framing vs path-coexistence conflict for maintainer ruling; did not force an unsound path-unification that risks the green vfork/dlopen/deep-fork battery.
- R2 3rd-cadence response (do trivial swaps in one batch): READ the coordinator (fork-process-continuation.ts) to enumerate actual backend call sites. Finding: the mechanical child-install collapse is ALREADY in place (coordinator calls coarse attachChild/attachBorrowedChild + driveRestoredPlan @829/847/1006/1010). Every REMAINING fine-grained backend call is interleaved with guest execution: beginUnwind@621 -> guest unwinds/throws (host-only run-loop catch) -> finishUnwindAndSerialize@663; beginParentReplay@727 -> host drives guest rewind DIRECTLY via invokeForkContinuationBegin(wpk_fork_rewind_begin)@729-734; beginChildReplay@888/beginBorrowedChildReplay@1075/beginAbort@755 same shape. Folding any into a coarse fm_parent_replay/fm_abort_replay = moving the guest wpk_fork_rewind_begin/abort_begin drive INTO the module via __wpk_fork_drive_table call_indirect = the carved-out abort/rewind group = GUEST-ABI GATE (drive-table eligibility) + guest-unwind hazard -> STOP+flag per maintainer carve-out. Conclusion: NO additional mechanical swaps remain to batch; the only collapse left IS the carved-out group. No R2 code landed. Reaffirmed NEEDS-DEFER-DECISION with concrete coordinator evidence.
- Fork control-flow INVERSION — parent replay/abort phase LANDED (3 commits on brandonpayton/rust-first-abi44-reconcile). Resolved the crux (Rust can't emit the ref-typed call_indirect drive) with the injector stub-replacement seam: coarse entries authored fully in Rust call a plain (ptr,i32)->() placeholder import env.__wpk_fork_drive_plan, and a new fork-module-inject pass inject_drive_thunk replace_imported_func's it into a local thunk forwarding to the injected fm_drive_execute shim (verified: placeholder import ABSENT from emitted wasm; fm_parent_replay/fm_parent_abort exported). Added fm_parent_replay/fm_parent_abort (begin_replay/abort_impl -> build_rewind_plan -> drive each activation's guest wpk_fork_rewind_begin/abort_begin via the drive table). Migrated the TS host: coordinator beginModuleParentReplay/beginModuleAbortReplay now bind the begin exports (bindActivationBeginDrive) + issue ONE coarse call, DELETING the per-activation invokeForkContinuationBegin loops; backend gains parentReplay/parentAbort/bindActivationBeginDrive + driveTable option (fine-grained beginParentReplay/beginAbort RETAINED for module-only unit tests). host-native + .mjs harness left on fine-grained exports (coarse entries ADDITIVE, no ABI break). Behaviourally identical to the old host loop; does NOT touch the twice-trapped mid-unwind seal (only starts the rewind of already-committed frames). fm_* exports 74->76 (additive — raw reduction is BLOCKED-ARGUED). Validation (Node): five end-to-end fork suites (fork-continuation/vfork-production-mechanism/fork-from-dlopen-side-module-e2e/fork-module-gc-replay/malloc-deep-fork) 18/18 PASS driving the coarse path through real forks; fork-module-backend-abort + multi-activation 4/4; host-native --lib 45/0; fork-codec 435/435; check-abi-version consistent NO bump (ABI-neutral, drive-table binds by name); tsc clean; build-wasm --verify-fresh both widths match. fork-instrument-coverage NOT a clean signal (pre-existing D-01 runFixture hang at parent; killed run corrupts program-index global-setup -> regenerate via cargo xtask build-deps program-index to repair). STOP-AND-ARGUE: fm_parent_seal_capture (SOUNDNESS WALL — needs a new DRIVE_OP_UNWIND_END + drives wpk_fork_unwind_end, the twice-trapped state machine; straddles the JS-only run-loop catch + seal-time-OOM reroute); raw 74->8-12 reduction (needs host-native + .mjs harness migration off fine-grained + capture/child folds); child-reconstruct rewind fold + exnref-gate (browser-gated reentrant-drive). Browser owed at Phase 6. Report: task-inversion-report.md.
- Fork control-flow INVERSION — capture-SEAL fold LANDED (the piece prior passes stopped-and-argued as a SOUNDNESS WALL). 4 commits on brandonpayton/rust-first-abi44-reconcile: c47735247 (fork-codec DRIVE_OP_UNWIND_END=9 + DRIVE_SLOT_UNWIND_END=7 + stride 7->8 + append_unwind_end_steps), 5174f238a (injector void_indirect_ty ()->() + DRIVE_OP_UNWIND_END shim branch, matched BEFORE >=REWIND_BEGIN since op 9 is also in that range), acd0b537e (fm_parent_seal_capture: build_seal_plan -> drive each guest wpk_fork_unwind_end via the injector seam -> finish_unwind_impl -> serialize_journal_alloc_impl; both wasm widths rebuilt+injected+restaged), d310fb23c (host: sealModuleCapture binds wpk_fork_unwind_end via bindActivationSealDrive + ONE sealCaptureAndSerialize call, deleting the per-activation unwind_end loop + finishUnwindAndSerialize; NORMAL assert -> post-drive sweep). RESOLVED the wall: wpk_fork_unwind_end is ()->() (emit_end_fn empty sig) so the shim needed a THIRD call_indirect type; done as an additive drive op. Avoided the 2 prior traps by keeping the FULL-SEAL path (fm_parent_seal_capture, COMPLETE capture only) DISTINCT from the PARTIAL-abort path (sealForAbort = fm_finish_unwind only, NO unwind-end drive) — neither drives unwind-end on a partial capture. Both host floors preserved: JS run-loop exception catch untouched (runs before seal; C-* fixtures pass), seal-time ContinuationAllocationError OOM reroute stays host-visible (fm_parent_seal_capture returns 0+errno on image-mmap fail -> typed throw -> abort-replay; P-11 passes). fm_* 76->77 (additive). ABI-NEUTRAL, NO bump (host<->module contract, drive table binds by name; check-abi consistent). Node validation: fork-codec 436/0, fork-module-inject 2/0, host-native --lib 45/0/4, fork-continuation+vfork+backend-abort+gc-replay 12/12, dlopen-e2e+malloc-deep-fork 9/9 (multi-activation dlopen exercises the new stride), backend-abort+multi-activation 4/4, fork-instrument-coverage P-*/C-*/S-*/K- 31/31 with --testTimeout=60000 (default-5s batch failures were cold-boot timeouts ~8.7s/fixture, NOT a hang — P-10 passes alone; D-* skipped for the PRE-EXISTING D-01 harness hang unrelated to fm_*). Rebuilt fork-module wasm (build-key 0e3fb408), ./run.sh local-build re-finalized the SourceOnly projection, xtask verify-fresh CLEAN (exit 0). Browser leg owed to the coordinator. Report: task-seal-capture-fold-report.md.
- Fork control-flow INVERSION — child-reconstruct REWIND-BEGIN fold LANDED (the browser-gated reentrant child-rewind drive, child-worker mirror of fm_parent_replay). 1 commit on brandonpayton/rust-first-abi44-reconcile: aad325649. Added fm_child_reconstruct (build_child_rewind_plan_impl -> drive each activation's guest wpk_fork_rewind_begin via the injector-wired __wpk_fork_drive_plan -> fm_drive_execute seam). REUSES DRIVE_OP_REWIND_BEGIN + DRIVE_SLOT_REWIND_BEGIN (same op/slot as parent replay), so NO fork-codec op, NO injector-shim branch, NO new slot. Unlike fm_parent_replay there is NO begin step: the child replay state was already seeded by fm_begin_child_replay/fm_add_activation_child_replay (COW) or the borrowed variants before this entry — fm_child_reconstruct folds ONLY the guest rewind drive. New per-activation ActivationFrames.child_rewind_root records the exact root the host loop drove wpk_fork_rewind_begin with: module_buffer for COW (rewind inherited continuation in place), child-private prefix for vfork BORROWED (active-frame-pointer write lands in private scratch, never the parked parent's prefix); set at all 4 construction sites, borrowed primary overrides the COW default after copy_borrowed_child_prefix. Migrated the TS host: attachModuleChild + attachBorrowedModuleChild now bindActivationBeginDrive + issue ONE backend.childReconstruct(), DELETING the per-activation invokeForkContinuationBegin(wpk_fork_rewind_begin) loops; REWINDING assert -> post-drive sweep. bindActivationBeginDrive reused (abort-begin bind harmless on child path). fine-grained beginChildReplay/beginBorrowedChildReplay/addActivation*ChildReplay seed wrappers RETAINED (they seed the state fm_child_reconstruct drives). fm_* 77->78 (additive). ABI-NEUTRAL, NO bump (drive table binds by name, reuses REWIND_BEGIN; check-abi consistent). Node validation: fork-from-dlopen-side-module-e2e + vfork-production-mechanism 9/9 (CORE: COW dlopen child + vfork borrowed child through childReconstruct); fork-continuation+backend-abort+multi-activation+gc-replay+malloc-deep-fork 13/13; fork-instrument-coverage P-*/C-*/S-*/K- 31/31 (--test-timeout=60000; D-* filtered for pre-existing D-01 harness hang); host-native --lib 45/0/4; fork-codec 436/0; build clean; xtask verify-fresh exit 0; build-wasm --verify-fresh both widths match (build-key 5fbc7e2f); ./run.sh local-build re-finalized the SourceOnly projection. STOPPED-AND-ARGUED: the exnref tag-validity gate move (task named it) is a CORRECTNESS/TIMING finding, NOT foldable into fm_child_reconstruct — the gate is an ADMISSION check that must run BEFORE the exnref materialize drive (driveRestoredPlan/DRIVE_OP_EXN @ fork-process-continuation.ts:886), but fm_child_reconstruct is the guest REWIND drive that runs LAST (@:966, after reconstruction); placing the check there validates the exnref AFTER it was already materialized (too late). Correct home = the reconstruction ENTRY (fm_attach_child), plan increment 5, needs a NEW additive per-activation exception-codec/tag seeding mechanism (fm_set_activation_exception_tags-style) + module-side re-check + corrupt-recipe EINVAL test + WebKit re-confirm; separable scope, not a soundness wall. Recommended as its own change; deferred for maintainer go/no-go. Browser leg (Chromium+WebKit) owed to the coordinator. Report: task-child-reconstruct-fold-report.md.
